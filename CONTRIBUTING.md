# Contributing to Loretide / AI 开发协作

本规范适用于所有人类与 AI 开发者。当前 GitHub 仓库保存项目文档；本地 app 是独立应用仓库，尚未发布代码基线。没有明确代码仓库和基线提交的代码任务不得标记 ready。

## 领取与状态

1. 只领取带 ready 标签、无负责人、依赖已关闭的 Issue。
2. 先评论执行者名称、工具、分支和开始时间；设置负责人并将 ready 改为 in-progress，然后重新读取 Issue 确认。共用 GitHub 账号时，评论中的执行者名称才用于区分 AI。
3. GitHub 标签/负责人操作不是原子抢锁。若发现其他执行者已领取或无法确定唯一所有者，停止修改，由主任务裁定。无写权限者仅申请领取，等待主任务确认。
4. 一个 Issue 一个执行者、一个独立分支/worktree。禁止同时改同一工作目录。
5. 阻塞时标记 blocked，记录复现、依赖和最小缺项；不要弱化测试或自行扩大范围。
6. 完成后创建 Draft PR，在描述写 Related to #编号（待审阶段不使用自动关闭关键字），添加 review-needed，移除 in-progress。
7. 主任务审查代码、证据及范围后决定补改或合并；合并并完成验收后由主任务关闭 Issue。执行者不自行合并、关闭 Issue。

## 每张 ready 任务必须具备

- 目标和不做事项；明确修改模块/文件及禁止范围。
- 代码仓库 URL、基线提交、分支要求；独立工作目录。
- 前置依赖、运行环境、独立测试数据库及端口要求。
- 可执行验收步骤、预期结果、浏览器证据要求。
- 数据保护、回滚方式、交付文件要求。

## 开发约束

阅读 AGENTS.md、应用 CLAUDE.md、docs/11、docs/12、docs/13 和 docs/design/README.md。历史建议服从最新确认；TODO不代表无代码，已有代码不代表已验收。

UI完整复用Multica组件、布局、字体和颜色/字号token。真实AI执行器保持禁用，除非后续专门任务明确通过安全验收并授权启用。不得读取、复制或上传模型客户端登录凭据。

保留未提交修改；禁止reset/clean、整目录覆盖、真实发布或未经授权删除数据。测试使用独立合成数据，不得将清理型测试连接到开发业务库。各执行者独立数据库和端口；共享服务重启/故障注入必须由主任务排定互斥时间窗口。

## 交付

PR包含问题与修复行为、修改模块、实际测试结果、浏览器证据、未覆盖项和回滚方法。记录写入records/并关联对应任务。禁止将密钥、数据库dump、运行日志原文、依赖目录或私人素材提交仓库。

GitHub负责任务与审查留痕，不自动唤醒AI。主任务通过各AI工具派发Issue链接并接收交付；后台轮询仅在实际配置后才能称为持续监控。
