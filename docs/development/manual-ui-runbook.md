# 诊断页手动验证 runbook

## 这份文件是什么

**一次跑完的操作脚本。** 它把六份手动清单的 **92 条**按「页面/标签页」重排，让你少来回切换，而不是按特性分六次跑。

分两段：**SOP 经营底座**（`§12`）与**诊断**（`§3`～`§9`）。两段用的数据、页面完全不重叠，可以分开跑。

| 来源 | 条数 | 编号前缀 | 段 |
|---|---:|---|---|
| `specs/004-lt009-brand-workspace/manual-ui-todo.md` | 11 | `004-W-x` / `004-S-x` / `004-T-x` | SOP |
| `specs/017-lt013-account-settings-page/manual-ui-todo.md` | 9 | `017-L-xx` | SOP |
| `specs/019-lt015-brand-precheck-switch/manual-ui-todo.md` | 8 | `019-P-x` | SOP |
| `specs/002-diag-package-stream-recovery/manual-ui-todo.md` | 37 | `002-V05-xx` / `002-V08-xx` / `002-V11-xx` | 诊断 |
| `specs/006-diag-trace-waterfall-regression/manual-ui-todo.md` | 20 | `006-W-x` / `006-V-x` / `006-L-x` | 诊断 |
| `specs/008-diag-linkage-and-invariants/manual-ui-todo.md` | 7 | `008-J-x` / `008-O-x` | 诊断 |

> **那六份文件仍是各自特性的权威来源。** 本 runbook 只是它们的执行顺序，不替代它们：条目的措辞、对应的 FR/SC、以及「这一条为什么存在」，以原文件为准；两边不一致时**以原文件为准**，并以更新本 runbook 的方式消除不一致。原文件**不删不改**。

编号在本 runbook 里带来源前缀（`004-W-1`、`017-L-01`、`019-P-1`），原文件里的编号不变（`W-1`、`L-01`、`P-1`）——加前缀只是为了在一张登记表里不撞号。

按 `.specify/memory/constitution.md` 原则 II，UI 验收**不写单测、不做自动点击**。本 runbook 的每一条都由人在真实浏览器里执行。

---

## 0. 先跑哪些

**`§12` SOP 经营底座优先。** 它是 W-02 的验收路径，当前优先级最高；`§3`～`§9` 的诊断段其次。两段不共用数据也不共用页面，先跑哪一段都不影响另一段，但时间有限时先跑 `§12`。

| 顺序 | 段 | 条数 | 什么时候跑 |
|---:|---|---:|---|
| 1 | **`§12` SOP 经营底座（W-02）** | 28 | **优先。** 每次 SOP 线有卡合入后 |
| 2 | `§3`～`§9` 诊断 | 64 | 诊断线有卡合入后，或需要一次完整回归时 |

### 跑之前：先把迁移应用上

**本机实例不会自己跑迁移**，启动脚本也不跑。拉过新代码之后不先应用迁移，新端点会以 `503` 回答且日志里看不出原因——`§12` 的账号页与自动预检开关都会这样。

```bash
cd server
go run ./cmd/migrate up
```

连接串、密码位置与「怎么确认已经到最新」见 **[Native Windows development](native-windows.md) 的「Migrations are not applied by the launcher」一节**——本节不重复它。

---

## 1. 前置条件

### 1.1 起一个本机实例

```powershell
./scripts/local-windows.ps1 start
./scripts/local-windows.ps1 status
```

启动的先决条件、端口、日志位置、失败时该看哪个文件，全部见 **[Native Windows development](native-windows.md)**——本节不重复它。只提两点与本 runbook 直接相关的：

- `start` 会在启动前逐项检查前置条件，失败时**打印该看的日志与下一步命令**。卡住时先读它的输出，不要直接重试。
- `§9` 的两条要用 `stop` / `start` 制造断线，所以这两个命令全程都要能用。

登录后打开 **Web `/{workspaceSlug}/diagnostics`**（例：`/loretide-dev-check/diagnostics`），需要 **workspace owner / admin** 身份。**桌面端未挂载诊断页**，本 runbook 不涉及桌面端。

浏览器**开发者工具的 Network 面板**要打开——`§5` 与 `§8` 的多条要看请求。

### 1.2 测试数据全部自造，不要找现成的

诊断页的「**运行模拟场景**」按钮就是造数工具。`§3` 会让你跑 6 次模拟，后面所有条目都用这 6 次运行，不需要任何预置数据。

**不要用开发库里已有的运行来跑本 runbook**：一是你不知道它们是什么形态，二是 `§9` 会让某张表不可写。

### 1.3 不得对开发数据跑清理类用例

`§9` 的两条（`002-V11-10`、`002-V11-11`）要人为让某张表不可写，属**破坏性操作**。

**只在一次性本机实例上执行，绝不对开发业务库运行**（`docs/development/ai-collaboration.md`：清理型测试不得连接开发业务库）。跑完这两条后，该实例的数据**不要再用于其他验证**——重新 `stop` / `start` 并重跑 `§3` 造一批干净数据。

### 1.4 记录方式

- 每条只有三种结果：**通过** / **未通过** / **未执行**。
- **未执行不填通过。** 数据形态不具备、环境不允许、时间不够——都记「未执行」并在备注写原因。
- 依赖特定数据形态的条目现在**都有造数场景**，见 `§3.1`。造出来了就跑，跑不出来记「未执行」并写下卡在哪一步——**不得记为通过**。
- 唯一仍标「**如无可跳过**」的是 `006-L-5`：它要的是「原故障运行已被保留期裁剪或跨工作区不可读」，那是保留期与授权的行为，不是模拟器能造的数据形态。造法见 `§3.1` 末尾。

---

## 2. 一次跑完的顺序

下表是**诊断段**（`§3`～`§9`）的顺序。SOP 段（`§12`）的顺序在那一节里单列，两段之间没有依赖。

| 节 | 页面 / 标签页 | 条数 | 为什么排在这 |
|---|---|---:|---|
| §3 | 故障复现 | 1 | 造数据，后面全靠它 |
| §4 | 运行列表 / 运行详情 | 26 | 四组（瀑布 / 四态 / 关联 / 跳转与对象版本）共用同一批运行，一个标签页里跑完 |
| §5 | 诊断导出 | 10 | 需要 Network 面板与一次终端比对，集中做 |
| §6 | 概览 | 7 | 只读，看指标 |
| §7 | 技术日志 · 筛选与分页 | 2 | 不涉及实时流，先做 |
| §8 | 技术日志 · 实时流 | 16 | 最长一节，要停/起 API、改角色、切标签页 |
| §9 | 破坏性 | 2 | **放最后**，跑完这批数据作废 |
| **诊断段合计** | | **64** | |
| §12 | SOP 经营底座 | 28 | **另起一段，见 `§0`**：不同页面、不同数据，与上表无依赖 |
| **全文合计** | | **92** | |

---

## 3. 造数据（故障复现标签页）

| 编号 | 操作 | 期望 | D13-V |
|---|---|---|---|
| — | 跑 `normal`、`slow`、`timeout`、`database`、`clock_skew` 各至少一次，记下各自 `run_id` | 每次都生成运行记录并跳到「运行详情」 | 前置 |
| — | 对其中任意一次运行点「复现」，再跑一次 | 新运行带 `original_run_id`，指向被复现的那一次 | 前置（`006-L-1`/`L-2` 要用） |
| **002-V08-01** | 选场景 `normal` 运行一次，记下 `run_id` | 生成运行记录，跳到「运行详情」 | V08（前置，`§5` 要用） |

---

### 3.1 诊断自验场景（造出「本来造不出」的数据形态）

故障场景下拉里除了 16 个业务故障，还有 **9 个 `shape_` 开头的诊断自验场景**。它们只在隔离实例（`APP_ENV=development` 且 `LORETIDE_DIAGNOSTICS_TEST=1`）里可选，生产路径拿不到。跑法与普通场景一样：选中，运行一次。

**顺序有讲究**：`shape_sink_failure` 放最后，`shape_deep` 放倒数第二。

| 步 | 场景 | 得到什么 | 解锁的条目 |
|---:|---|---|---|
| 1 | `shape_concurrent` | 两根**可见重叠**（重叠约 200ms，各占半根）的横条，互不为父子 | `006-W-3` |
| 2 | `shape_orphan` | 一个显示为顶层**并标注「父 span 不在本次运行内」**的 span。若只见顶层、没有标注，那是缺陷，记「未通过」 | `006-W-5` |
| 3 | `shape_single_span` | 单行瀑布，不空图、不异常宽 | `006-W-6` |
| 4 | `shape_not_run` | 运行列表的回归结果列显示「未运行」 | `006-V-1` |
| 5 | `shape_regression_failed` | 显示「未通过」 | `006-V-3` |
| 6 | `shape_undecidable` | 显示「无法判定」，且两个原始值同时可见 | `006-V-5` |
| 7 | `shape_no_module` | 关联信息里 `module` 显示「未记录」 | `006-L-4`（`module` 那一半） |
| 8 | `shape_deep` | 250 个 span：瀑布出现「另有 N 条」折叠提示，失败步骤及其各级父节点在折叠状态下仍可见；点开后层级完整 | `006-W-7`、`006-W-8`、`008-O-3` |
| 9 | `shape_sink_failure` | 概览的 `sink_errors` 与 `dropped` 都 > 0 | `002-V11-01`、`002-V11-02`、`002-V05-15` |

**第 8 步会明显变慢**，是正常的：一次运行写 250 条技术事件，每条一个事务。**不要刷新**——刷新会拿到一个写了一半的运行。

**第 9 步不需要重建实例。** 失败只作用于那一次运行的写入；紧接着跑一次 `normal`，两个计数就不再增长。这一点本身值得确认一下（对应 `SC-005`）。

#### 两条不靠模拟器

| 条目 | 怎么造 |
|---|---|
| `008-O-2`（追踪内事件都没有版本） | **不需要新场景**。点面板的「触发页面错误（测试）」：它写出的事件不带对象版本且自带全新追踪号，正是这条要的形态。跳进那条追踪即可 |
| `006-L-5`（原故障运行不可读） | 不是数据形态，是**保留期与授权**的行为。用 `LORETIDE_DIAG_RETENTION_DAYS=1` 让被引用的运行过期，或从另一个工作区去引用它。都做不到才记「未执行」 |

#### 两条已知界限

- **导出只带 100 条技术事件。** `shape_deep` 有 250 个 span，但诊断包的导出上限是 100 条，所以导出文件里看不到全部。这是既有行为，不是本次交付的缺陷，`002-V08-x` 的导出条目不受影响。
- **`002-V11-02` 的 `dropped` 来源。** 第 9 步走的是**写入失败**路径，不是条目原文括注的「缓冲区溢出」路径。指标数值是真的。想看满环路径：**连跑 4 次 `shape_deep`**（4 × 250 条，缓冲环容量 1000）。

### 3.2 另外四条：保留期路径、授权路径，与一条已被覆盖的

主任务 2026-09-15 的预检报告把 19 条记为「模拟器造不出」，其中 4 条不在 `§3.1` 的九个场景里。**它们都不是数据形态**，裁决如下。

| 条目 | 属于 | 怎么造 |
|---|---|---|
| `008-J-4`（追踪事件已过保留期） | **保留期路径** | 与 `006-L-5` 同一处理：`LORETIDE_DIAG_RETENTION_DAYS=1` 让被引用的追踪事件过期，再点「查看追踪」。跳转本身要成功，事件表为空列表并有空态提示 |
| `002-V05-14`（保留期外记录不可读） | **保留期路径** | 同上。让一批记录过期后，对照概览的 `retention_days` 与技术日志最早一条的时间 |
| `002-V08-11`（他人 workspace 的 `run_id`） | **授权路径** | 需要在 `handler-tests` 工作区有**成员身份**，从那里取一个 `run_id`，再回到当前工作区试预览与下载。**没有成员身份就记「未执行 · 缺少授权前置」**，不是数据问题 |
| `002-V11-08`（> 200 条实时事件） | **已被 `§3.1` 覆盖** | 打开实时流，跑一次 `shape_deep`：250 个事件一次写入，正好越过「仅显示最近 200 条」的界 |

前三条**不该由模拟器承担**，理由与 `006-L-5` 相同：让模拟器造一条「已过期的记录」或「他人工作区的运行」，等于让它写一条本不该存在的记录。第四条只是此前没人发现 `shape_deep` 顺带解决了它。

---

## 4. 运行列表 / 运行详情（26 条）

### 4.1 trace 瀑布 — 运行详情 → trace 标签页（9 条）

| 编号 | 操作 | 期望 | D13-V |
|---|---|---|---|
| **006-W-1** | 打开一次含父子 span 的运行 | 子 span 相对父 span 缩进一级；层级与 `parent_span_id` 一致 | V03 / V09 |
| **006-W-2** | 观察横向位置 | 每行左边距对应其相对本次运行最早时刻的偏移；行与行之间的空白对应真实等待间隙 | V03 |
| **006-W-3** | 找两个时间重叠且互不为父子的 span | 二者在横轴上**可见地重叠**，不是排成前后两行等长条 | V03 |
| **006-W-4** | 打开 `clock_skew` 场景的运行 | 出现「时钟偏差」标注并提示该处不可按时间解读；位置**照实显示**，未被夹取到父区间内 | V03 |
| **006-W-5** | 打开一次 span 的 `parent_span_id` 指向本次运行之外的运行 | 该 span 显示为顶层并标注「父 span 不在本次运行内」；**未被丢弃、未挂到其他父节点下** | V03 |
| **006-W-6** | 打开一次只有单个 span 的运行 | 正常显示单行，不出现空图或异常宽度 | V03 |
| **006-W-7** | 打开一次 span 数 > 200 的运行（**如无可跳过并注明**） | 显示「另有 N 条」折叠提示；**失败步骤及其各级父节点在折叠状态下仍可见** | V03 |
| **006-W-8** | 点开 `W-7` 的折叠提示 | 被折叠节点展开，层级仍然正确，无悬空片段 | V03 |
| **006-W-9** | 打开一次**含至少两层父子且有一处并发**的运行，**计时**：从看到瀑布起说出「哪一步最慢」与「哪段等待最长」 | **10 秒内可答出**，且**不需要**打开原始 JSON 或开发者工具。答不出或需要看 JSON → 记未通过，并说明卡在哪（缩进不明显？时间轴看不出间隙？） | V03 |

> **`006-W-9` 是 SC-001 的唯一验收路径。** 纯函数测试能证明偏移与耗时算得对，证明不了「人能不能在 10 秒内看出来」。**不得**用任何自动测试代替它宣布 SC-001 通过。

### 4.2 四态回归判定 — 运行列表 → 回归结果列（5 条）

| 编号 | 操作 | 期望 | D13-V |
|---|---|---|---|
| **006-V-1** | 查看一条从未评估的运行（`regression` 为空） | 显示「未运行」——**不是空白单元格**，也不是通过 | V09 |
| **006-V-2** | 查看一条通过的运行 | 显示通过，且同行或详情处可见 `expected_code` 与 `actual_code` 作为判定依据 | V09 |
| **006-V-3** | 查看一条失败的运行 | 显示未通过 | V09 |
| **006-V-4** | 阅读「通过」旁的说明文案 | 表达「**模拟结果符合该故障预期**」，**不**表达「该功能可用」 | V09 / V12 |
| **006-V-5** | 若存在 `status` 与 `regression` 冲突的运行（**如无可跳过并注明**） | 显示「无法判定」，并同时可见两个原始值 | V09 |

### 4.3 回归关联 — 运行列表（5 条）

| 编号 | 操作 | 期望 | D13-V |
|---|---|---|---|
| **006-L-1** | 查看 `§3` 里那条由复现而来的运行 | 原故障运行、场景、模块、代码版本**四项均可见** | V09 |
| **006-L-2** | 点击原故障运行 | **在同一诊断页内**切换到该运行详情；浏览器地址栏**未新增路由段** | V09 |
| **006-L-3** | 查看一条首次运行（`original_run_id` 为空） | 标注「本身即原故障」——**不是**「未记录」，也不是空白 | V09 |
| **006-L-4** | 查看一条 `module` 或 `build` 为空的运行（**如无可跳过并注明**） | 该项显示「未记录」——不是空白、不是推断值 | V09 |
| **006-L-5** | 若原故障运行已被保留期裁剪或不可读（**如无可跳过并注明**） | 标注「原故障运行不可读」并给出原因类别；点击**不进入错误页或空白页** | V09 |

### 4.4 关联跳转与对象版本 — 操作时间线 / 单次追踪（7 条）

与 `§4.1` ～ `§4.3` 共用同一批运行，**不需要额外切换页面**。`008-J-3` 是 008 修掉的一个真实缺陷，请重点验。

> **2026-09-15 实施期修正，原措辞与实现不符**：`008-J-1` / `008-J-4` 原写「切到**技术日志**」，实际落点是**单次追踪**页。主任务 2026-09-15 裁决**改规格不改落点**——单次追踪本就是正确目标。
>
> 同时修掉由此暴露的真 bug：该页的渲染条件曾是 `tab === 3 && !runId && trace`，而「查看追踪」**必然**设置运行编号，于是走 `008-J-1` 这条路进来时 **`008-O-1` / `O-2` / `O-3` 的「对象与版本」卡片永远不显示**——上一轮只能靠刷新页面手输追踪编号才看到它。**这三条在本次修复前实际不可验收**，这一轮请连着 `008-J-1` 一起复验。（`specs/011-diag-panel-time-and-jump`）

| 编号 | 操作 | 期望 | D13-V |
|---|---|---|---|
| **008-J-1** | 在操作时间线上找一条**带追踪编号**的事件，点「查看追踪」 | 切到**单次追踪**页；该页**同屏**显示瀑布、「对象与版本」卡片与事件表，只含该次追踪；筛选**均已清空**；分页回到第一页。**不需要刷新页面，也不需要手输追踪编号**——`008-O-1` 紧接着就在同一屏上跑 | V02 |
| **008-J-2** | 先设好严重级与时间范围，再点另一条事件的「查看追踪」 | 上一次的筛选**不残留**，结果只由本次事件决定 | V02 |
| **008-J-3** | 找一条**没有追踪编号**的事件 | 「查看追踪」呈**不可用**并给出说明；点击**不切页**、**不**显示一屏无关日志。**改动前这里会静默跳到未筛选的全量日志**——若仍如此即为未通过 | V02 |
| **008-J-4** | 用 `LORETIDE_DIAG_RETENTION_DAYS=1` 让一条追踪的事件过期（造法同 `006-L-5`，见 `§3.1`），再对引用它的事件点「查看追踪」 | 切到**单次追踪**页成功，事件表为**空列表**并有空态提示——与 `008-J-3` 的「不可跳转」在观感上可区分 | V02 |
| **008-O-1** | 跳进一条**带对象版本**的追踪，看事件表上方的「对象与版本」卡片 | 显示对象类型与标识，并按**先后顺序**列出本页出现过的版本，每个带出现次数，不重复 | V02 |
| **008-O-2** | 点「触发页面错误（测试）」，再跳进它写出的那条追踪——该事件不带对象版本（见 `§3.1`） | 显示「未记录版本」，**不显示空白**、**不伪造**版本号 | V02 |
| **008-O-3** | 跑一次 `shape_deep`（`§3.1` 第 8 步，250 个事件同属一个对象，分页 25 → 约 10 页），在其中一页查看该对象 | 只显示**当前页内**的版本，且卡片下方有范围说明（「仅统计本页出现过的版本」）。**确认这句说明确实能让读者看懂这不是全部版本**——那是「不新增数据读取路径」的已知代价，不是缺陷 | V02 |

---

## 5. 诊断导出（10 条）

**整节都要开着 Network 面板。** `002-V08-05` 需要一个终端。

| 编号 | 操作 | 期望 | D13-V |
|---|---|---|---|
| **002-V08-02** | 选中 `§3` 的那个 run，点「预览导出内容」 | 显示 manifest 列表与 `limits` 裁剪说明 | V08 |
| **002-V08-03** | 点「下载脱敏诊断包」 | 浏览器保存一个文件；文件名与响应头 `Content-Disposition: attachment; filename="..."` 一致（Network 面板核对） | V08 |
| **002-V08-04** | 用文本编辑器打开下载的文件 | 键名为**线上格式**：`run_id` / `workspace_id` / `has_more`（**不是** `runId` / `workspaceId` / `hasMore`）；`"redacted": true`；`limits` 列出被裁剪项 | V08 |
| **002-V08-05** | 终端：<br>`curl -X POST -H "X-Workspace-ID: <ws>" -H "Authorization: Bearer <token>" "http://127.0.0.1:18000/api/content-diagnostics/export?run_id=<id>" -o expected.json`<br>然后 `cmp expected.json <下载文件>` | `cmp` **无输出**（逐字节一致） | V08 |
| **002-V08-06** | 检查下载文件内容 | 无源文本、无凭据 / token、无模型 provider stdout、无媒体副本；敏感字段为脱敏值 | V08 |
| **002-V08-07** | 改请求查询串加 `account_id=x`（或用他人 workspace 的 `run_id`）触发下载 | 返回 403 / 404 诊断错误对象；界面显示「导出失败，未生成文件：`<CODE>` · `<next_action>`」；**浏览器未生成任何文件** | V08 |
| **002-V08-08** | 在预览与下载**全过程**中观察 Network 面板的外发请求 | 除本实例 API 外**无任何外部地址请求**；诊断包不自动上传 | V08 |
| **002-V08-09** | 下载过程中让会话过期（清除 token）后再点下载 | 返回授权错误对象；**不生成半个文件** | V08 |
| **002-V08-10** | 跑一个会产生错误证据的场景（`model_auth` / `database`），预览并下载该 run 的诊断包 | **负例检查**：包内不出现 prompt / 源文本 / token / provider stdout / 文件正文；`safe_message` 只出现固定枚举文案；`limits` 列出被裁剪项 | V08 |
| **002-V08-11** | 用**他人 workspace** 的 `run_id` 触发预览与下载（**两条路径都试**）。`run_id` 从 `handler-tests` 工作区取——需要在那个工作区有成员身份，见 `§3.2` | 两条路径都返回 403 / 404 诊断错误对象；界面显示 `next_action`；不生成文件、不泄漏该 run 的任何字段 | V08 |

---

## 6. 概览（7 条）

只读，看指标。`002-V05-14` 需要在概览与技术日志之间看一次。

| 编号 | 操作 | 期望 | D13-V |
|---|---|---|---|
| **002-V11-01** | 在 `sink_errors > 0` 的状态下查看指标区 | `sink_errors` 可见并显示真实数值；组件状态**不显示为全部健康** | V11 |
| **002-V11-02** | 在 `dropped > 0`（缓冲区溢出丢弃）的状态下查看指标区 | `dropped` 可见并显示真实数值 | V11 |
| **002-V11-03** | 查看 `retention_days` 与 `capacity` | 如实显示；保留期为 0 或容量为 0 的异常配置**照实展示，不伪装为正常** | V11 / V05 |
| **002-V11-04** | 停止 web 心跳（关闭诊断页 ≥30 秒后由另一入口查看概览） | 心跳过期的组件状态**不显示为 `healthy`** | V11 |
| **002-V05-14** | 用 `LORETIDE_DIAG_RETENTION_DAYS=1` 让一批记录过期（造法同 `006-L-5`，见 `§3.1`），再对照概览的 `retention_days` 与技术日志中最早一条的时间 | 早于保留期的记录已不可读取；从早于保留期的游标续读时出现缺口提示（见 `002-V11-05`） | V05 |
| **002-V05-15** | 在 `dropped > 0` 的状态下查看概览，再回到技术日志 | 概览的 `dropped` 显示真实数值；界面**不把被丢弃的区间显示为完整** | V05 |
| **006-V-6** | 查看概览 | **不应**出现四态的聚合计数——本功能明确不做。**若出现即为越界** | V09 |

> `002-V11-01` / `V11-02` / `V05-15` 需要 `sink_errors` 或 `dropped` 大于 0。跑一次 **`shape_sink_failure`**（`§3.1` 第 9 步）即可，两个指标同时变为非 0。**不再需要等 `§9` 的破坏性操作**，这三条现在可以和 `§6` 概览的其余条目在同一轮里跑完。

---

## 7. 技术日志 · 筛选与分页（2 条）

| 编号 | 操作 | 期望 | D13-V |
|---|---|---|---|
| **002-V05-11** | 在 `events` 请求的查询串上加 `account_id=x`（当前 `Scope.Accounts` 为空集合，任何非空值都应被拒） | 返回 403 诊断错误对象；界面显示拒绝与 `next_action`；**不返回任何事件** | V05 |
| **002-V05-13** | 点「下一页」直到 `has_more` 为假，再点「回到开头」 | 每次翻页取回不重复的下一批；`has_more` 为假时「下一页」不可点；「回到开头」把游标归零并重新从头列出 | V05 |

---

## 8. 技术日志 · 实时流（16 条）

最长的一节。中途要 `stop` / `start` API、改一次角色、切一次标签页——**按下表顺序做，不要跳**，`002-V05-06` 依赖 `V05-04` 造出的断线状态。

| 编号 | 操作 | 期望 | D13-V |
|---|---|---|---|
| **002-V05-01** | 点「连接 / 继续实时流」，连续观察 **3 分钟**（≥6 次服务端 25 秒轮换） | 状态行**始终** `connected`；全程**无任何**断线提示 Alert | V05 |
| **002-V05-02** | 观察这 3 分钟内的 `stream?...` 请求 | 每约 25 秒一条新 `stream` 请求；上一条的最后一行 NDJSON 含 `"rotate":true` | V05 |
| **002-V05-03** | 设 `severity=error`，保持实时流连接 | 新到事件均为 error；每次续连的查询串都含 `severity=error` 且 `after=<上次游标>` | V05 |
| **002-V05-04** | `./scripts/local-windows.ps1 stop` 停止 API | **≤10 秒**内出现断线提示「实时流已断开，正在从上次游标重新连接。」或「实时流连接失败…」 | V05 |
| **002-V05-05** | 保持 API 停止，观察重连请求的时间间隔 | 间隔按 **2s → 4s → 8s → 16s → 30s** 递增，之后稳定在 30s，**不再每 2 秒重试** | V05 |
| **002-V05-06** | `start` 恢复 API | **≤5 秒**清除断线提示；状态回到 `connected`；筛选**仍为** `severity=error`；事件从停机前游标续读，**无重复、无跳号** | V05 |
| **002-V05-07** | 点「暂停实时流」，等几秒后再点「连接 / 继续实时流」 | 状态变 `paused` 后**不再发起请求**；继续后从暂停时游标续读，期间事件（保留期内）不丢失 | V05 |
| **002-V05-09** | 流连接中切到其他标签页 **≥1 分钟**（触发浏览器定时器节流）后切回 | 回到前台后**立即续连一次**，不需等待完整退避；状态恢复 `connected` | V05 |
| **002-V11-05** | 清理技术日志（或等待保留期裁剪）后从旧游标续读，使服务端返回 `gap=true` | 状态行出现「· 检测到保留期缺口」与缺口提示 | V11 / V05 |
| **002-V11-06** | 缺口出现后继续接收若干正常批次 | 缺口提示**仍然保留**，不因后续正常批次自动消失 | V11 |
| **002-V11-07** | 点「清除缺口提示」 | 缺口标示与提示消失；后续再出现缺口时**重新出现** | V11 |
| **002-V11-08** | 打开实时流后跑一次 `shape_deep`（`§3.1` 第 8 步，250 个事件一次写入） | 显示「仅显示最近 200 条」；列表条数有界，**不无限增长** | V11 |
| **002-V11-09** | 构造同一批内含重复 `event_id` 与乱序 `sequence` 的事件 | 每个 `event_id` **只出现一次**；显示顺序按 `sequence` 单调递增 | V11 |
| **002-V05-10** | 流连接中切换 workspace | 事件列表清空、游标归零、缺口提示复位；**不残留**上一个 workspace 的事件 | V05 |
| **002-V05-12** | 在 `stream` 请求的查询串上加 `account_id=x` | 流不建立（连接前 403）；进入 `denied` 态并**停止自动重连** | V05 |
| **002-V05-08** | 流连接期间，在另一浏览器/账号把当前用户的 workspace 角色从 owner/admin 降为 **member** | 流关闭并显示授权失败提示，含 `next_action`（如 `check_authorization`）；**不再自动重连**（Network 面板无新 `stream` 请求） | V05 |

> **`002-V05-08` 放在本节最后**：它会把你降权到 member，之后概览与导出都不可用。跑完记得把角色改回 owner/admin。


> **`002-V05-03` 的口径冲突已解决**（主任务 2026-09-15 裁决，`fix(views): stream copy says filters follow, matching the implementation`）。
>
> 界面实时日志区块的标题曾写「不应用上方历史筛选」，而实现一直是**筛选跟随**（`useDiagnosticStream` 把 `component` / `severity` / `errorCode` / `traceId` / `from` / `until` 传给流，`stream-state.ts` 有 `filter-change` 转移）。裁决是**改文案不改实现**：文案现已改为「沿用上方筛选」，与本条要求一致。
>
> 跑本条时按正常口径判定即可：设 `severity=error` 后，新到事件均为 error，且每次续连的查询串都含 `severity=error`。**文案与条目不再互相矛盾。**

---

## 9. 破坏性验证（2 条）— 放在最后

> **只在一次性本机实例上执行。绝不对开发业务库运行。**
>
> 这两条跑完后，本实例数据作废：重新 `stop` / `start` 并重跑 `§3` 再做别的验证。

| 编号 | 操作 | 期望 | D13-V |
|---|---|---|---|
| **002-V11-10** | 让技术日志写入失败（例如把 `content_technical_log` 置为不可写），然后执行一次正常业务操作并跑一次模拟 | **业务操作照常成功**，不报错、不回滚；概览的 `dropped` / `sink_errors` 上升，失败被记为**有界的丢弃**而不是中断 | V11 |
| **002-V11-11** | 让关键审计写入失败（例如把 `content_operation_audit` 置为不可写），然后点导出下载（导出会写一条 audit） | **整个操作按事务拒绝**：返回诊断错误对象、不生成文件、**不留下半条记录**；与 `002-V11-10` 的「丢弃」形成对比 | V11 |

这两条是一对：**同一类失败，两种正确反应**。普通技术日志失败要被吞掉并计数，关键审计失败要让整个操作失败。跑完对照一下两者的界面表现是否真的不同。

---

## 10. 结果登记表

**未执行不填通过。** 三列只勾一个；未通过与未执行都要写备注。

| 编号 | 通过 | 未通过 | 未执行 | 备注（未通过的现象 / 未执行的原因） |
|---|:---:|:---:|:---:|---|
| 002-V08-01 | ☐ | ☐ | ☐ | |
| 006-W-1 | ☐ | ☐ | ☐ | |
| 006-W-2 | ☐ | ☐ | ☐ | |
| 006-W-3 | ☐ | ☐ | ☐ | 跑 `shape_concurrent`（`§3.1` 第 1 步） |
| 006-W-4 | ☐ | ☐ | ☐ | |
| 006-W-5 | ☐ | ☐ | ☐ | 跑 `shape_orphan`（`§3.1` 第 2 步）；只见顶层、没有标注 = 未通过 |
| 006-W-6 | ☐ | ☐ | ☐ | 跑 `shape_single_span`（`§3.1` 第 3 步） |
| 006-W-7 | ☐ | ☐ | ☐ | 跑 `shape_deep`（`§3.1` 第 8 步，250 span）；这一步会慢，不要刷新 |
| 006-W-8 | ☐ | ☐ | ☐ | 依赖 W-7 的同一次 `shape_deep` 运行 |
| 006-W-9 | ☐ | ☐ | ☐ | **SC-001 唯一验收路径**；未通过时写清卡在哪一步 |
| 006-V-1 | ☐ | ☐ | ☐ | 跑 `shape_not_run`（`§3.1` 第 4 步） |
| 006-V-2 | ☐ | ☐ | ☐ | |
| 006-V-3 | ☐ | ☐ | ☐ | 跑 `shape_regression_failed`（`§3.1` 第 5 步） |
| 006-V-4 | ☐ | ☐ | ☐ | |
| 006-V-5 | ☐ | ☐ | ☐ | 跑 `shape_undecidable`（`§3.1` 第 6 步） |
| 006-L-1 | ☐ | ☐ | ☐ | |
| 006-L-2 | ☐ | ☐ | ☐ | |
| 006-L-3 | ☐ | ☐ | ☐ | |
| 006-L-4 | ☐ | ☐ | ☐ | `module` 半：跑 `shape_no_module`（`§3.1` 第 7 步）；`build` 半：不设 `LORETIDE_BUILD` |
| 006-L-5 | ☐ | ☐ | ☐ | 不靠模拟器：`LORETIDE_DIAG_RETENTION_DAYS=1` 让原运行过期，或跨工作区引用（`§3.1`） |
| 002-V08-02 | ☐ | ☐ | ☐ | |
| 002-V08-03 | ☐ | ☐ | ☐ | |
| 002-V08-04 | ☐ | ☐ | ☐ | |
| 002-V08-05 | ☐ | ☐ | ☐ | |
| 002-V08-06 | ☐ | ☐ | ☐ | |
| 002-V08-07 | ☐ | ☐ | ☐ | |
| 002-V08-08 | ☐ | ☐ | ☐ | |
| 002-V08-09 | ☐ | ☐ | ☐ | |
| 002-V08-10 | ☐ | ☐ | ☐ | |
| 002-V08-11 | ☐ | ☐ | ☐ | 授权路径（`§3.2`）：需 `handler-tests` 工作区成员身份；没有就记「未执行 · 缺少授权前置」 |
| 002-V11-01 | ☐ | ☐ | ☐ | 跑 `shape_sink_failure`（`§3.1` 第 9 步） |
| 002-V11-02 | ☐ | ☐ | ☐ | 跑 `shape_sink_failure`（`§3.1` 第 9 步）；走的是写入失败路径，不是满环 |
| 002-V11-03 | ☐ | ☐ | ☐ | |
| 002-V11-04 | ☐ | ☐ | ☐ | |
| 002-V05-14 | ☐ | ☐ | ☐ | 保留期路径（`§3.2`）：`LORETIDE_DIAG_RETENTION_DAYS=1` |
| 002-V05-15 | ☐ | ☐ | ☐ | 跑 `shape_sink_failure`（`§3.1` 第 9 步） |
| 006-V-6 | ☐ | ☐ | ☐ | 出现聚合计数即未通过（越界） |
| 002-V05-11 | ☐ | ☐ | ☐ | |
| 002-V05-13 | ☐ | ☐ | ☐ | |
| 002-V05-01 | ☐ | ☐ | ☐ | 需连续观察 3 分钟 |
| 002-V05-02 | ☐ | ☐ | ☐ | |
| 002-V05-03 | ☐ | ☐ | ☐ | |
| 002-V05-04 | ☐ | ☐ | ☐ | |
| 002-V05-05 | ☐ | ☐ | ☐ | |
| 002-V05-06 | ☐ | ☐ | ☐ | |
| 002-V05-07 | ☐ | ☐ | ☐ | |
| 002-V05-09 | ☐ | ☐ | ☐ | |
| 002-V11-05 | ☐ | ☐ | ☐ | |
| 002-V11-06 | ☐ | ☐ | ☐ | |
| 002-V11-07 | ☐ | ☐ | ☐ | |
| 002-V11-08 | ☐ | ☐ | ☐ | 打开实时流后跑 `shape_deep`（`§3.1` 第 8 步） |
| 002-V11-09 | ☐ | ☐ | ☐ | |
| 002-V05-10 | ☐ | ☐ | ☐ | |
| 002-V05-12 | ☐ | ☐ | ☐ | |
| 002-V05-08 | ☐ | ☐ | ☐ | 跑完把角色改回 owner/admin |
| 002-V11-10 | ☐ | ☐ | ☐ | **破坏性**，一次性实例 |
| 002-V11-11 | ☐ | ☐ | ☐ | **破坏性**，一次性实例 |
| 008-J-1 | ☐ | ☐ | ☐ | |
| 008-J-2 | ☐ | ☐ | ☐ | |
| 008-J-3 | ☐ | ☐ | ☐ | |
| 008-J-4 | ☐ | ☐ | ☐ | 保留期路径（`§3.2`）：`LORETIDE_DIAG_RETENTION_DAYS=1` |
| 008-O-1 | ☐ | ☐ | ☐ | |
| 008-O-2 | ☐ | ☐ | ☐ | 点「触发页面错误（测试）」后跳进该追踪（`§3.1`）；不需要模拟器场景 |
| 008-O-3 | ☐ | ☐ | ☐ | 跑 `shape_deep`（`§3.1` 第 8 步） |
| **诊断段小计 64** | | | | 通过 __ / 未通过 __ / 未执行 __ |
| **SOP 段（`§12`）** | | | | |
| 004-W-1 | ☐ | ☐ | ☐ |  |
| 004-W-2 | ☐ | ☐ | ☐ |  |
| 004-W-3 | ☐ | ☐ | ☐ |  |
| 004-W-4 | ☐ | ☐ | ☐ |  |
| 004-W-5 | ☐ | ☐ | ☐ |  |
| 004-W-6 | ☐ | ☐ | ☐ | 需要一个本次之前就存在的老工作区 |
| 004-W-7 | ☐ | ☐ | ☐ | 「选了上海」与「没选过」只差一个「默认」标记 |
| 019-P-1 | ☐ | ☐ | ☐ |  |
| 019-P-2 | ☐ | ☐ | ☐ |  |
| 019-P-3 | ☐ | ☐ | ☐ | 关闭后刷新仍须为关；翻回开即未通过 |
| 019-P-4 | ☐ | ☐ | ☐ |  |
| 019-P-5 | ☐ | ☐ | ☐ |  |
| 019-P-7 | ☐ | ☐ | ☐ |  |
| 019-P-8 | ☐ | ☐ | ☐ |  |
| 017-L-01 | ☐ | ☐ | ☐ |  |
| 017-L-02 | ☐ | ☐ | ☐ |  |
| 017-L-03 | ☐ | ☐ | ☐ |  |
| 017-L-04 | ☐ | ☐ | ☐ | 409 撞不出来记「未复现」，不要勾通过 |
| 017-L-05 | ☐ | ☐ | ☐ |  |
| 017-L-06 | ☐ | ☐ | ☐ |  |
| 017-L-07 | ☐ | ☐ | ☐ |  |
| 017-L-08 | ☐ | ☐ | ☐ |  |
| 017-L-09 | ☐ | ☐ | ☐ | 待裁定项，不是测试：记「未执行 · 待裁定」 |
| 019-P-6 | ☐ | ☐ | ☐ | 发现任何预检控件即未通过，并写清它在哪一段 |
| 004-S-1 | ☐ | ☐ | ☐ |  |
| 004-S-2 | ☐ | ☐ | ☐ |  |
| 004-S-3 | ☐ | ☐ | ☐ |  |
| 004-T-1 | ☐ | ☐ | ☐ |  |
| **SOP 段小计 28** | | | | 通过 __ / 未通过 __ / 未执行 __ |
| **全文合计 92** | | | | 通过 __ / 未通过 __ / 未执行 __ |

### 登记时的三条规则

1. **未执行不填通过。** 没跑就是没跑，哪怕你确信它会过。
2. **未通过要写清现象**，不要只写「不对」。`006-W-9` 尤其：写清是缩进看不出层级、还是时间轴看不出间隙。
3. **三列加起来必须等于 92**（诊断段 64 + SOP 段 28）。对不上说明漏了条目。
4. **两段可以分开登记。** 只跑了 `§12` 就只填 SOP 段那 28 行，诊断段留空并在小计处注明「本轮未跑」——不要为了凑数把没跑的填成未执行以外的任何一列。

---

## 11. `specs/008` 的 7 条：已并入

**并入日期**：2026-09-14。**实现 PR**：`feat(diagnostics): audit-to-trace linkage and regression invariants`（008 实施）。

7 条已按本节原定口径并入：新增 **`§4.4 关联跳转与对象版本`** 排在 `§4.3` 之后（与 `§4` 共用运行详情与事件列表，不需额外切换页面），结果登记表加 7 行，合计由 **57** 改为 **64**。

`specs/008-diag-linkage-and-invariants/manual-ui-todo.md` 仍是这 7 条的权威来源；两边不一致时以原文件为准。

**7 条全部「未执行」。** 008 的实现 PR 只交付了自动测试与界面改动，界面行为按原则 II 必须由人在真实浏览器里确认。
---

## 12. SOP 经营底座（W-02）验收（28 条）

SOP 线三张已合并卡的手动项。**当前优先级最高的一段**，见 `§0`。

| 来源 | 条数 | 编号前缀 | 对应验收 |
|---|---:|---|---|
| `specs/004-lt009-brand-workspace/manual-ui-todo.md`（LT-009） | 11 | `004-W-x` / `004-S-x` / `004-T-x` | AC-001、D11-V01 |
| `specs/017-lt013-account-settings-page/manual-ui-todo.md`（LT-013） | 9 | `017-L-xx` | D11-V02、R-005～R-008 |
| `specs/019-lt015-brand-precheck-switch/manual-ui-todo.md`（LT-015） | 8 | `019-P-x` | D11-V06、R-038 / R-039 |

> **这三份文件仍是各自特性的权威来源。** 本节只是它们的执行顺序，条目的措辞、对应的 FR/SC 与「为什么存在」以原文件为准；两边不一致时**以原文件为准**。原文件**不删不改**。

### 其余 SOP 卡为什么没有手动项

**LT-010（工作区授权）、LT-011（账号与平台配置）、LT-012（人设版本）、LT-014（账号范围偏好）、LT-016 没有界面改动**，能力全部落在服务端与 core 纯函数，由自动测试覆盖，**不需要手动项**。这不是漏写：`specs/014` / `015` / `016` / `018` 四个目录下都没有 `manual-ui-todo.md`，因为按原则 II 需要手验的只有界面行为。

### 前置

1. 已按 `§0` 跑过 `go run ./cmd/migrate up`。
2. 以 **workspace owner / admin** 登录。
3. 手头要有**一个本次之前就存在的老工作区**（`004-W-6` 要用）。
4. 开发者工具 Network 面板打开（`017-L-04` / `L-05` 要看请求）。

### 顺序说明

页面分组是「建工作区 → 设置页工作区标签 → `/accounts` → 切换隔离」。**这与任务卡里写的「设置页 → onboarding → /accounts」次序不同**，原因是依赖：`004-W-3` / `W-5` / `W-7` 与 `019-P-5` 都要先有 A、B 两个工作区，而那两个是 `004-W-1` / `W-2` 在建工作区流程里造出来的。把设置页放前面会导致设置页被访问两次，比换一次顺序切得更多。

---

### 12.1 建工作区（onboarding，2 条）

| 编号 | 操作 | 期望 | 对应验收 |
|---|---|---|---|
| **004-W-1** | 创建工作区 **A**，时区选 `Asia/Shanghai` | 表单有「时区」选择，**默认已预选** `Asia/Shanghai`；创建成功 | AC-001 / D11-V01 |
| **004-W-2** | 再创建工作区 **B**，时区选 `America/Los_Angeles` | 创建成功，B 的时区为所选值 | AC-001 / D11-V01 |

---

### 12.2 设置页 → 工作区标签（12 条）

一次进来跑完。前 5 条是时区，后 7 条是自动预检开关，同一张卡片上下相邻。

| 编号 | 操作 | 期望 | 对应验收 |
|---|---|---|---|
| **004-W-3** | **刷新页面**，分别打开 A、B 的设置 → 工作区 | A 显示 `Asia/Shanghai`，B 显示 `America/Los_Angeles`，**各自不变** | AC-001 / D11-V01 |
| **004-W-4** | 退出并重新登录，再看 A、B | 时区仍分别为上述值 | AC-001 / D11-V01 |
| **004-W-5** | 在 A 里把时区改为 `Europe/London` 并刷新 | 显示 `Europe/London`；**B 不受影响**；A 里已有内容的时间戳**没有变化** | AC-001 / D11-V01 |
| **004-W-6** | 看一个**本次之前**就存在的老工作区 | 时区显示 `Asia/Shanghai` 并带「**默认**」标记；**不显示空白** | D11-V01 |
| **004-W-7** | 在 A 显式选择 `Asia/Shanghai` 后再看 | 仍显示 `Asia/Shanghai`，但**不再有「默认」标记**——「选了上海」与「没选过」要能分辨 | D11-V01 |
| **019-P-1** | 打开一个**新建**品牌的设置 | 「自动预检」一行在时区行下面，开关为**开**；旁边**没有**「关闭不表示免人审」那句 | D11-V06 / R-038 |
| **019-P-2** | 把开关关掉 | 保存成功；开关旁**出现**「关闭不表示免人审」 | D11-V06 / R-038 |
| **019-P-3** | 关闭状态下**刷新页面** | 仍为**关**。**不得**被默认值翻回开——本段最容易出错的一条 | D11-V06 |
| **019-P-4** | 再打开开关 | 保存成功；那句说明**消失** | D11-V06 / R-038 |
| **019-P-5** | 品牌 A 关、品牌 B 开，来回切换查看 | 各自保持各自的值，互不影响 | D11-V06 / R-039 |
| **019-P-7** | 用一个**非管理员**成员登录后查看该行 | 开关可见但**不可点**（与同页其它字段一致的禁用表现） | D11-V06 |
| **019-P-8** | 关掉自动预检后，检查**同品牌的时区**设置 | 时区不变。两个键同在一个 settings blob 里，写一个不能抹掉另一个 | D11-V01 / D11-V06 |

---

### 12.3 `/accounts` 页（10 条）

手输地址 `/{workspaceSlug}/accounts` 进入——**本阶段侧栏没有入口**，见 `017-L-09`。

| 编号 | 操作 | 期望 | 对应验收 |
|---|---|---|---|
| **017-L-01** | 建账号 A（显示名「A 号」），再建 B（换一个平台，显示名「B 号」） | 空状态文案可读（不是空白页）；两个都出现在列表里；创建后新账号被选中；**没有手动刷新** | D11-V02 / R-005 |
| **017-L-02** | 选中 A，人设框里打「半截草稿」**不保存**，点 B；再点回 A | B 的框里是 B 的内容或空，**一个字符都不来自 A**；回到 A 时是**服务端的内容**，草稿已经没了（这是设计，不是缺陷） | D11-V02 / R-006 |
| **017-L-03** | 给 A 写一段人设并保存，按 F5；再换一个浏览器或无痕窗口登录同一账号 | 内容还在且版本号与保存后一致；换浏览器后仍在——证明它在服务端不在本地 | D11-V02 / R-007 |
| **017-L-04** | 保存三态：(a) 正常保存；(b) 两个标签页几乎同时保存同一账号；(c) 断网后保存 | (a)「已保存」；(b) 至少一次出现「**有人同时改了这个账号，再保存一次即可**」，**这句里没有「失败」二字**，再点一次能成功；(c)「保存失败」，恢复网络后能再保存 | D11-V02 / R-007 |
| **017-L-05** | 给 A 连改三次人设（每次都保存），然后看 B | B 的人设内容与版本号**纹丝不动**（B 若从未保存过，仍显示「还没有保存过版本」） | D11-V02 / R-008 |
| **017-L-06** | 把工作区的插件功能关掉，重新进入 `/accounts` | 页面照常打开；创建、编辑平台/显示名、编辑并保存人设**全部可用** | D11-V02 |
| **017-L-07** | 给一个新账号**不填任何人设**直接保存；再清空一个已有人设并保存 | 两次都保存成功并出现版本号，**不是**「请填写」之类的拦截 | D11-V02 |
| **017-L-08** | 并排打开 `/settings`（工作区标签）与 `/accounts`，**同一主题、同一缩放、同一字号偏好** | 卡片圆角、分隔线、行高、标签列宽度、保存态的图标与字号**一致**；选中的账号在悬停时**没有被降级成普通 hover 的样子** | D11-V02 |
| **017-L-09** | 侧栏是否需要「账号」入口 | **这是一条待裁定项，不是测试。** 本阶段没有入口，只能手输地址。加不加、加在哪一组、什么图标与措辞由主任务定；登记时记「未执行 · 待裁定」 | — |
| **019-P-6** | **从头到尾通读** `/accounts` 页所有控件 | **没有任何**与自动预检相关的开关、勾选框或下拉——一个都不能有。这是 SOP「不提供账号级覆盖」的界面侧负例 | D11-V06 / R-039 |

---

### 12.4 切换隔离与术语（4 条）

放最后：要在 A、B 之间来回切，且 `004-S-2` 会用到诊断页。

| 编号 | 操作 | 期望 | 对应验收 |
|---|---|---|---|
| **004-S-1** | 在 A 建一条可见对象（任务 / Issue），切到 B | B 的列表**不出现** A 的对象 | AC-001 |
| **004-S-2** | 在 B 打开诊断页实时流，再切回 A | 流按新工作区重建，**不混入** B 的事件 | AC-001 |
| **004-S-3** | 切回 A | A 的数据完整显示，无需重新创建 | AC-001 |
| **004-T-1** | 中文界面下看创建、切换器、设置三处 | 一律称「**工作区**」；**不出现**「品牌空间」作为界面用词 | D11-V01 |

> **`004-S-1` ～ `S-3` 验的是上游既有机制**（查询键含 `wsId`、查询按 `workspace_id` 过滤），不是 LT-009 新建的。发现任何缺 `wsId` 的查询键或未按 `workspace_id` 过滤的接口，**作为独立缺陷报告主任务**，不在该特性内修。

---

### 这一段里最该认真跑的三条

1. **`019-P-3`**（关闭后刷新仍为关）——对应「把布尔的 `false` 当成未设置」那类缺陷，症状是用户关掉预检、第二天发现它自己开了。
2. **`019-P-6`**（账号页无任何覆盖控件）——SOP 明写「不提供账号级覆盖」，发现任何一个控件都记「未通过」并写清它在哪一段。
3. **`004-W-7`**（选了上海 vs 没选过）——两者显示同一个值，只差一个「默认」标记；标记丢了不会有人立刻发现。
