# Docker 开发实例启动与凭据边界

用户授权开始独立 Docker 开发环境，并询问是否会将本机 Codex 登录状态复制到 VPS。

## 已执行

以指定密钥及已验证主机文件登录 opsadmin，创建 /home/opsadmin/loretide-dev，传输应用源码和 Git 元数据。浅克隆 bundle 导入失败后，改用源码归档及保留 shallow 文件的 Git 元数据传输；git fsck 通过。Windows Git 配置产生换行状态差异，远程尚需规范化检查，不能声称双端 Git 完全干净。

创建 loretide-dev Compose 项目及独立网络、数据库卷、Go 缓存。数据库不映射宿主端口；Web/API 绑定回环 13000/18000；各容器有 CPU/内存限制。远程生成专用数据库密码和 JWT 密钥，保存在权限600的忽略文件；未打印秘密。

镜像已拉取，配置解析通过。数据库健康；Web 安装依赖后启动，服务器本地 /login 返回200。API 首次依赖下载/编译中，尚未通过健康检查，不将容器运行中当作启动验收通过。现有 Multica 与其他容器仍运行，未对它们执行管理操作。

本机创建 SSH 隧道的工具调用被自动审批以 blocked by policy 拒绝；隧道没有建立，没有绕过。真实浏览器验证、登录、API鉴权、迁移重跑和恢复测试尚未完成。

## 凭据边界

没有读取或传输本机 Codex/Claude 登录文件。Compose 没有 daemon 服务，没有挂载本机 home、登录目录、SSH 私钥或 Docker socket。未来本机 daemon 在本机调用已登录客户端，VPS 接收授权任务成果，不获取客户端登录令牌。

上游存在凭据链接/复制及权限适配代码，尚未完成安全改造；没有将“设计上不传令牌”表述为所有上游路径已安全。真实 daemon 配对、模型执行仍不得作为已验收能力启用。

## 文件关联

- [Compose 配置](../app/compose.loretide-dev.yaml)：实际独立实例定义，应用本地提交486d909。
- [远程开发说明](../app/docs/development/remote-docker.md)：启动/状态/停止、数据保留与凭据规则，应用本地提交26faec7；文件已传到远程，远程尚未同步该后续提交历史。
- [任务清单](../tasks/todo.md)：现有任务状态不提前完成；环境部分启动不代表 LT-004～008 全部通过。
- 本记录：保存本轮实施证据、失败和剩余边界。应用未配置 GitHub origin；根文档仓库同步不等于应用源码已推送远程 GitHub。
