# Docker 服务联通与迁移复核

用户要求继续。使用指定 opsadmin 密钥和已验证 known_hosts，只读核验独立实例并重跑幂等迁移。

## 本轮实测通过

- loretide-dev 的 db 健康，API/Web 运行。
- 服务器回环18000的 /health 返回200；13000的 /health 经 Web 代理返回200。
- 未认证请求 /api/workspaces 返回401；这不能代替有效会话和完整权限验收。
- `go run ./cmd/migrate up` 重跑返回 Done，已有迁移跳过；schema_migrations 为496条。部分上游条件迁移记录不代表每条 SQL 实际执行，不能仅凭数量声称所有扩展具备。
- 使用实际实例配置解析成功；以空环境文件解析时缺少必需密钥而失败，负例通过。
- 远程 Web 运行生成了 next-env.d.ts 一行变动；其余先前差异为换行状态。没有覆盖或清理远程变动。

## 未完成与阻塞

计划停止本实例 API、重启本实例数据库再验证恢复，但 SSH 认证失败：`agent refused operation`，随后 `Permission denied (publickey)`。失败发生在登录之前，远程没有执行这些停启命令。需要恢复该密钥在 SSH agent 中的签名可用性；不改用 root 或密码。

此前创建本机 SSH 隧道被自动审批拒绝，尚未建立；本轮未绕过或重试该被拒绝操作。浏览器登录/刷新/断线、有效会话、空库迁移对照、数据停启保留以及真实执行器安全仍未验证。

## 关联与状态

- [上轮部署记录](2026-09-13-Docker开发实例启动与凭据边界.md)：本轮将其中 API 健康待验证更新为已通过的新证据，保留历史记录。
- [任务清单](../tasks/todo.md)：LT-003/005/006/007 标为进行中，不提前宣称完成全部验收。
- 本记录：操作证据与阻塞；未读取或复制 AI 登录状态，未调用模型。
