# GitHub 任务派发到独立 Codex 窗口

用户授权安排其他聊天窗口领取开发任务。本轮创建两个独立任务，实际配置均为 gpt-5.6-terra / medium，未唤醒原暂停 Sol 窗口。

| Issue | 聊天 | 线程 ID | 独立端口 Web/API/PG |
|---|---|---|---|
| #1 | Loretide DEV-01 Windows 独立初始化 | 01a09ac2-5f1c-7760-9b59-eb0bdb8aae4d | 13101/18101/15401 |
| #2 | Loretide DIAG-REVIEW-01 诊断独立验收 | 01a09ac2-6291-7d92-821b-53c7769724ad | 13102/18102/15402 |

派发包含：app-main 固定基线 bb3a4b4、独立 clone/分支、Issue 认领核对、文件范围、禁止修改主开发实例、不得读取模型凭据、真实执行禁用、Draft PR 回报主任务。领取状态由执行者核对并写 GitHub，创建聊天不等于已领取或已交付。

模型选择沿用当前任务分工：Terra Medium 执行明确边界的开发与测试；疑难权限/跨模块/恢复竞态向主任务提供证据后，再决定 Sol High。此为任务配置选择，不宣称官方性能比较结论。

主任务通过线程状态与交付消息接收结果；本轮没有创建后台定时监控。相关产物在各自 Issue、应用任务分支和 records；本文只记录派发配置。

协调纠正：DEV01 曾误建重复任务 01a09ac2-b583-7293-a12b-0f9eb8fddedf。主任务核验其已归档，最新回合 interrupted；不恢复。已要求原执行者重读 Issue 归属避免冲突。

用户指出派发与 GitHub 状态必须同步。本轮 live 核验 #1/#2 均已由执行者设置 in-progress、assignee 899ms，ready 已移除。主任务补充唯一执行线程确认评论，并更正 #2 评论把主任务 ID 当作执行线程的歧义。CONTRIBUTING.md 新增派发前预占规则。

主任务审查 PR #3（57e03c03f）：部分交付暂不合并，要求实际前端测试、零测试检测、数据库身份/权限核验、环境恢复；已向原执行线程发送补改指令。Issue #2 从 review-needed 改回 in-progress，保留唯一执行者；浏览器与 DB 依赖 #1 后续继续。

DEV01 中间报告：独立 15401 PostgreSQL 与 18101 API health=200，Web 13101 返回 500，未提交 PR。主任务要求核查实际 Next 解析版本/启动模式/cwd/继承环境，不能从 required-server-files.json 缺失直接断定依赖冲突；仅操作自己的进程，保留数据库与生成改动副本，不 reset/clean。浏览器验收继续未完成。

后续审查：DIAG02 提交 c409c5355 已补前端行为测试与 DB 门禁；主任务要求预期 Go 测试明确 pass、不接受 skip，并防 URI 查询覆盖核验目标。DEV01 已证实脚本误启 root Next15 而非 Web Next16，授权其在既定范围修正入口及临时文件绝对路径、清理仅本轮生成文件和轮换测试密码，继续独立浏览器验收，保持 Terra Medium。

DEV01 报告精确临时文件清理、独立测试密码轮换及实例重启被安全策略拒绝，均未执行。主任务要求提供脱敏拒绝理由与目标范围，禁止换窗口绕过同一拒绝；暂停相关变更，继续允许的只读检查和交付整理。浏览器验收保持未完成。

DIAG02 报告 PR #3 更新至 3c0f4f795：Go 必须 run/pass 各一次、无 skip/fail，DB URI 仅允许 sslmode，停止输出原始错误 JSON；报告 8 Go 与 2 Vitest 通过。主任务要求 #2 标 blocked 保留负责人，等待 #1 后续环境，未合并或关闭。

DEV01 已提供 CreateProcess Rejected / blocked by policy 原文。主任务不跨窗口重试被拒操作；#1 改 blocked 保留负责人。要求仅以四个精确源码/文档路径整理部分 Draft PR，禁止 add .，排除临时秘密和生成文件；若发布也被拒则停止。临时文件清理/密码轮换/重启及真实浏览器仍未执行。

DEV01 部分交付 PR #6：https://github.com/899ms/loretide/pull/6 ，提交 c08e216bb，报告仅含 bootstrap 脚本/测试与说明/记录四文件。保持 Draft、blocked，不合并；更正入口后的浏览器实测仍未完成，拒绝动作不重试。

用户要求可视区分窗口：将 DEV01 标题改为 窗口1 · Issue #1 · Windows初始化，将 DIAG02 改为 窗口2 · Issue #2 · 诊断验收。两者原本创建为 projectless 任务，不是 Codex 原生 worktree 任务；独立检出不等于 UI worktree 标志，不伪称有原生工作树标签。本轮不移动窗口或修改工作树。

主任务审查 PR #6 发现 PID 归属、环境恢复、Web upstream 与幂等性缺陷，已实际切换窗口1至 gpt-5.6-sol/high，继续原 PR 的代码/模拟测试修复。blocked 仍指真实运行验收被 policy 阻塞；不得重试被拒操作。窗口2继续等待。

用户指出窗口2闲置，主任务恢复其无环境依赖开发：验收脚本自身的 mock 行为测试（零测试/skip/身份/参数/环境恢复/脱敏），保持 Terra Medium，在原 Issue #2 / PR #3 内推进。Issue 加 in-progress 并保留 blocked 表示仅 DB/浏览器子项等待 #1。

DIAG02 更新 PR #3 至 51786705b，新增模拟行为测试。主任务代码审查发现默认 Vitest runner 原生 stdout 可能混入 JSON 返回，fake runner 尚未覆盖默认路径；要求补 stdout 隔离、默认路径回归、畸形 URI 脱敏和实际用户不符用例，再执行无 DB 验证。仍不合并，不执行 #1 被拒动作。

DIAG02 报告 PR #3 更新 dfc050475：默认 Vitest 输出分离、URI泛化错误与userinfo分拆、用户不符用例已补，模拟及实际无DB验证通过；结果由执行者报告，主任务核对提交及文件范围，未独立复跑，DB/browser仍未完成。
