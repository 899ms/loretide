# Hetzner 远程开发环境核验

用户提供目标主机、opsadmin 用户、专用 SSH 私钥路径和已验证 known_hosts 文件，要求禁止 root 或密码登录。

## 已执行

以 opsadmin 使用指定密钥连接，设置 IdentitiesOnly、BatchMode、StrictHostKeyChecking，并禁用密码及键盘交互认证。未读取/复制私钥内容，未使用 root SSH 登录。

只读检查身份、系统、资源、工具、监听端口与运行容器。sudo 仅用于读取容器列表和权限；没有安装、停止、重启或修改服务。

## 现场结果

- Ubuntu 26.04.1 LTS，x86_64，4 CPU。
- 内存 7.6 GiB，检查时 available 约 6.0 GiB；无 swap。根盘约 59 GB 可用。
- Git、Docker 29.8.0、Compose v5.5.1 可用；Go、Node、pnpm、Corepack、psql 未在当前远程 PATH 找到，不代表其他用户或容器没有安装。
- opsadmin 未取得普通 Docker 查询结果；通过 sudo -n 可列出容器，具备免密 sudo。后续容器管理可使用 sudo，不需要 root SSH 登录。
- 现有 Multica Web/API 监听回环 3000/8080，另有独立 PostgreSQL 容器。还有其他用户服务运行，均为现有资产。
- /home/opsadmin/loretide-dev 尚不存在。

## 下一步方案（尚未配置）

采用独立目录 /home/opsadmin/loretide-dev，独立 Compose 项目 loretide-dev、网络与数据库卷，不复用现有 Multica 数据库。建议 Web/API 使用回环 13000/18000，启动前再次检查；数据库仅容器内部访问。浏览器经 SSH 隧道访问。

Go/Node/pnpm 放入项目开发容器或独立工具目录，不要求本机安装全部服务。构建前核验镜像/版本与资源限制，串行构建以降低对现有服务影响。凭据保存在未跟踪的实例配置中，不进入文档或 Git。

LT-003 仍待完成：需要创建具体配置并验证解析、端口和路径隔离；数据库启动归 LT-005。没有运行应用、迁移或模型测试。

## 关联文件

- [任务清单](../tasks/todo.md)：LT-003～008 的独立环境、数据库、API、浏览器和生命周期验收。
- [远程衔接记录](2026-09-13-线索来源确认与远程开发衔接.md)：此前待指定服务器，本轮目标已提供并成功连接。
- 本文件：保存实际核验证据及未实施边界，不保存登录密钥或业务配置内容。
