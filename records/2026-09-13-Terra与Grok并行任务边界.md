# Terra 与 Grok Bot 并行任务边界

日期：2026-09-13

## 本次确认

Grok Bot 所在 VPS 负责原生开发测试环境的守护、恢复与验收；GPT-5.6 Terra（Medium）负责 Loretide 应用仓库内诊断功能的代码收口与浏览器验收。两条任务可以并行，但不得修改同一类文件、服务配置或运行实例。

## Terra 任务

- 收口诊断页面下载、实时流、游标续读、错误与暂停状态；补足测试和浏览器证据。
- 仅操作应用仓库 `app/` 中已有诊断相关模块及相应任务记录。
- 不改原生服务守护、Tailscale、Node/Go 安装、持久化恢复脚本、端口或 supervisor 配置。

## Grok Bot 任务

- 验收并完善原生环境服务守护、重启恢复、持久化与一键恢复说明。
- 不改 `app/packages/core/content`、`app/packages/views/content`、`app/server/internal/content/diagnostics` 的业务实现及前端页面。

## 关联文件

- `tasks/diagnostics.md`：Terra 开发状态与验收项。
- `records/2026-09-13-诊断下载与实时流收口.md`：诊断下载和实时流阶段记录。
- `scripts/native-env.sh`、原生服务管理文件：Grok Bot 环境任务的范围。
- `docs/11-首版开发基线与实施清单.md`：需求、计划和已验证状态的判定基线。

## Grok Bot 连接方式修正

Grok Bot 直接在其获赠 VPS 的本机终端执行任务，不使用 SSH、Tailscale SSH 或外部连接命令。`100.109.104.61` 仅作为浏览器验收访问地址；任务提示不得把它描述为需要建立的远程连接。

## Grok Bot 后续可并行任务

已完成：supervisord 对 API 与 Web 的进程级恢复演练，诊断数据保留验证，以及原生恢复脚本。

优先后续任务：

1. 可控整机重启后的持久化与恢复验收；当前记录未覆盖该场景。
2. 将“平台启动后手动执行 restore”收口为明确、可重复、可观测的恢复流程，不承诺无法由平台提供的开机自启能力。
3. 将原生环境的状态、健康、日志采集整理为只读诊断包和故障排查 Runbook。
4. 核对恢复脚本的幂等性、失败回滚和最小运行时依赖；禁止清库或覆盖源码。
5. 在不改应用业务代码的前提下，给出 API/Web 冷启动耗时、资源占用和日志保留建议。

## Grok Bot 完整后续委派

委派目标扩展为原生开发环境可靠性与可观测性收口：整机重启验收、Update 后恢复演练、服务状态和日志诊断、恢复脚本幂等与失败处理、性能基线、操作 Runbook。仍不涉及应用业务和诊断功能代码，避免与 Terra 冲突。

## Terra 收口回报核对（截图）

截图显示 Terra 已在不改 supervisor、网络、端口、持久化或恢复配置的前提下完成本轮诊断下载与实时流收口：新增流回归与下载回归，已运行 Core/Web/Views typecheck、两项 Vitest、诊断 Go 测试、定向 handler 测试和内容边界扫描。完整 handler 测试仍受既有测试数据库外键与删除清单漂移影响，不能视为通过。下一段适合进行诊断全量验收与既有失败隔离，不再扩展环境任务。

## 主任务调度机制修正

主任务负责向 Terra 派发指令、通过任务状态等待与读取交付审查结果、根据审查再发送下一条指令。Terra 的完成消息不会自动注入主对话；主任务必须主动监听并读取。已审查本轮 DIAG-08/DIAG-12 交付，接受其定向验收，未接受完整 handler 测试为通过；已直接向 Terra 派发下一项：既有 handler 测试夹具/清理失败的根因隔离及最小测试范围修复。

## Grok Bot 主任务纳入调度

用户授权主任务操作当前正在运行的 Grok Bot，并将其作为环境子执行器纳入调度。当前可见电脑控制面仅暴露 Chrome 与 Codex in-app 浏览器，未暴露 Grok Bot 客户端的可交互 surface；主任务因此不会打断其正在运行的环境任务。待可读取其完成结果时，主任务审查后再直接派发下一项。

## Grok Bot 桌面端接入确认

主任务已通过桌面计算机操作定位并只读查看 Grok Bot（开发测试bot）窗口。它正在执行环境任务第2-7部分：supervisor 冷启动验证、恢复脚本双跑、Update Runbook、doctor 诊断脚本、性能基线和验收文档。当前不插入新消息或中断，完成后由主任务读取交付、审查并直接派发后续任务。

## Terra 第二轮与环境门槛衔接

Terra 第二轮已完成根因隔离：首次 handler 全量的 `agent_owner_id_key` 出现在并发夹具清理，排除并发后稳定阻塞变为测试数据库 `127.0.0.1:15332` 拒绝连接。Terra 未做不安全修复，未修改生产代码、测试夹具、删除清单、supervisor、端口或数据库配置；诊断模块测试与边界扫描通过，handler 全量未标记通过。下一门槛由 Grok Bot 的环境稳定性验收解决。主任务已实时核对 Grok Bot 仍在工作，正执行 supervisor 冷启动验证、恢复脚本双跑、Update Runbook、doctor 脚本、性能基线与验收文档，未向其插入新消息。

## Grok Bot 轮询状态（16:37 后）

主任务再次通过桌面端读取：开发测试bot 的状态仍为“工作中”。最近一条进度消息为 16:37：后台连续进行 supervisor 冷启动验证、恢复脚本双跑、Update Runbook、doctor 诊断、性能基线与验收文档；尚未出现最终交付或需要输入的提示。主任务继续等待其完成事件，不中断。

## Codex Windows computer-use 通道纠正

本轮读取官方 bundled computer-use SKILL.md 及 guidance/api/confirmations，通过 node_repl 导入 @oai/sky 成功定位 Grok Bot 并读取开发测试bot。无需 Orca。此前未核对该技能即声称必须使用 Orca，现纠正。当前 UI 显示工作中，最新消息仍为16:37；仅凭工作中标签不能证明后台有持续进展。本轮未发送消息或中断任务。此前“持续监控”实际上为逐轮人工触发读取，未建立后台定时监控，不应称作持续监听。

## Grok 环境交付与 Terra 续派

用户提供 Grok 最终截图：报告 API/Web/PG RUNNING、诊断页200、诊断运行3条保留；恢复脚本双跑、Web单服务恢复、doctor与Runbook完成。整机重启和真实Update未验证，冷启动仍需手动restore。以上为执行器报告，尚非主任务独立验收。

主任务已直接向 Terra 续派：核对数据库稳定和测试库隔离后单实例重跑handler全量；不重启服务，不在业务库执行清理型测试；整理远端代码可审查补丁及新增测试，避免成果只留VPS。要求完成后通过任务消息向主任务提交待审交付。

## 独立测试数据库门槛

Terra 主动回报当前 DATABASE_URL 指向 loretide_dev，未执行清理型全量测试。主任务认可该停止决定，续派在现有PG中新建独立测试库/角色、核验全部测试连接并以合成fixtures初始化；仅进程级注入测试连接，不改开发库、共享环境配置或服务，不复制开发数据。若隔离或权限无法保证须回报。完成后主动向主任务发送交付。

## 工作空间删除契约决策

独立库已稳定复现三个content诊断表未分类。主任务查验docs/13：禁止普通诊断清理删除批准/审计证据，并未规定整个workspace显式删除后的永久留存；上游manifest对activity_log和channel_inbound_audit采取workspaceDelete。决定三个诊断表在既有workspace显式删除流程中按workspace_id、同一事务删除，不detach；普通日志过期仍不得删除审计。已授权Terra最小生产删除图/manifest修复，测试仅独立库合成数据，覆盖跨workspace保留和事务回滚，不执行实际开发库删除。待代码/测试交付审查，尚未验收通过。

## 主任务生产删除修复首轮审查

已读取远端测试和保护脚本及子任务ADR。报告handler全量通过，主任务尚未最终验收。发现失败注入trigger/function只在成功路径清理，中途Fatal会污染测试库；已打回补立即注册的唯一对象清理。另要求核对无FK诊断写入与workspace删除并发孤儿风险、473/502迁移记录差异，准确说明保护脚本为显式入口检查而非所有go test的强制保护。

## 删除与诊断写入竞态升级

子任务回报测试对象cleanup已修正并定向通过；主任务接受该项。确认diagnostics写入未参与workspace删除行锁协议，无FK的三表可出现删除后孤儿。按用户此前授权的跨模块/并发问题升级规则，已实际将同一开发任务设置为gpt-5.6-sol/high，授权最小公共事务guard及确定性双事务回归，要求覆盖run/audit/technical全部写入、失败语义和邻居workspace隔离。不操作开发数据或环境配置。整体删除修复仍待并发修复后审查。
