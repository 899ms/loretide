# 开发环境迁移与 Grok 私网接入评估

## 旧 VPS 清理与服务恢复

用户明确要求删除旧机开发部署并恢复原服务。78.47.43.8 使用 opsadmin 密钥登录，先停开发 API/Web，导出 PostgreSQL 和附件到本机临时迁移目录，再校验 SHA256。

- 数据库：662440b01b68a79acff7910218cbb2bbebcc056f59b77fdf9d25e6e2dd801119。
- 附件：aa80182d61a30ff3a60c535c7a680cab618874d43588ab2230df559b1da66011。
- 删除 loretide-dev 的 API/Web/DB 容器、database/go_build/go_modules 卷、default 网络，以及已核实绝对路径 /home/opsadmin/loretide-dev。删除旧机临时导出文件。
- 删除开发专用 golang:1.26.6-bookworm、node:22-bookworm 镜像。pgvector:pg17 被原 Multica 使用，保留。
- 按原停服清单恢复 11 个容器，全部 Up；有健康检查的 PostgreSQL、Karakeep Web、Neko 显示 healthy；Multica 网页 3000 和 API 8080/health 均返回 200。不代表所有服务业务操作逐项验收。
- 停止本机指向旧开发实例的 SSH 隧道进程 26420；原停服清单作为历史文件保留。

## 新 VPS 已执行状态

78.47.42.189 按用户指定使用 root 密码交互登录，密码不写文件。部署目录 /root/loretide-dev。

已安装 Ubuntu 软件包 Docker 29.1.3、Compose 2.40.3；上传应用源码和备份，校验一致；恢复 PostgreSQL 成功，启动 API/Web/DB 三容器，数据库 healthy。重新生成独立数据库密码和 JWT secret，配置文件权限 600。未传入本机 AI 登录状态。

首次安装依赖及编译在启动后继续。后续上传 Git 元数据、建立新 SSH 隧道与 HTTP 检查被用户中断，最终状态未收回，不标记可用性验证完成。不可直接重跑一次性部署脚本覆盖目录或再次恢复数据库。

应用文档已改为新机路径，应用本地提交 9a3b4c1；应用仓库没有 GitHub origin，不能把文档库推送当作应用源码备份。

## Grok Bot 接入评估

用户随后询问 Grok Bot 托管环境替代开发 VPS，说明没有外部连接地址并提供社区 Tailscale 方案截图。截图中的固定用户名 box 及服务持久性尚未独立验证。

可考虑私网 SSH：本机和远端加入可互访的 tailnet；远端必须有可用 SSH 服务和认证，或正确启用 Tailscale SSH 及其访问规则。相同账号登录不单独保证 SSH 可用。无需把 SSH 22 端口暴露到公网。

仍需实际核对远端允许安装/运行 Tailscale、服务是否持续存活、磁盘持久性、资源与 Docker/原生依赖权限；不能仅凭网络打通认定它可替代开发服务器。此轮未安装 Tailscale、未加入私网、未迁移到 Grok 环境。本机 PATH 未发现 tailscale 命令，不等同于确认未安装 GUI。

参考：https://tailscale.com/docs/features/tailscale-ssh 。现有 Hetzner 开发实例保留，待替代方案验证。

## 后续确认：原生开发与接入状态

用户明确要求 Grok Bot 环境不使用 Docker，采用原生开发方式。后续检查和部署围绕 Go、Node/pnpm、PostgreSQL，不将 Docker 作为前置条件。既有 Hetzner 部署本轮未改动。

最新截图中的 Bot 回复建议 Tailscale 私网加普通 SSH，用户名 box，使用用户在该环境设置的密码；并明确 openssh-server 和双方 Tailscale 尚待安装。这是截图中的自述，不是已执行或已连通的证据。尚无私网地址、主机指纹或 SSH 实测结果；也未证明磁盘和后台进程持久性。下一步由远端安装启动 SSH/Tailscale，用户完成账号授权，再获取私网 IP 和主机公钥指纹。本机随后接入并验证，完成前不迁移项目。

## 文件关联

- app/docs/development/remote-docker.md：当前新机部署路径与登录方式。
- app/compose.loretide-dev.yaml：独立容器配置。
- 本机 C:/Users/13900/AppData/Local/Temp/loretide-migration-20260913/：数据库/附件迁移备份、源码归档、无密码的一次性 deploy.py 和 connect.py；不提交含业务数据的备份到 Git。
- 本文件：本次迁移实际结果、未完成验证及私网方案边界。
