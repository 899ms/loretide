# 附件持久化与新开发服务器准备

## 本次进展

- 旧开发实例 78.47.43.8：明确设置 LOCAL_UPLOAD_DIR=/workspace/server/data/uploads，沿用已有目录，实际宿主机路径为 /home/opsadmin/loretide-dev/server/data/uploads。通过原有源码绑定挂载保留，Git 忽略 data/。
- Compose config --quiet 通过；写入本次专用标记文件后只重建 API，环境变量和标记内容读取均通过，随后删除该测试文件；API /health 返回 200。Web 和数据库未重建，其他容器未恢复。
- 这是附件目录和持久化配置检查，不代表上传接口、授权或浏览器附件验收完成。LT-003 保持进行中。
- 外部制作媒体仍由操作人在本机手动管理；本次未加入自动扫描、上传或强制托管。

## 新服务器与授权变化

用户指定新开发 VPS 78.47.42.189，沿用现有 SSH 密钥；随后说明新机尚无 opsadmin，明确授权先通过 root 密码登录初始化。密码不记录。

首次严格 SSH 检查发现该地址不在已验证主机文件中；未登录。Windows ssh-keyscan 遇到 KEX 兼容错误，改用 PuTTY plink 未认证握手获取公开指纹：

`ssh-ed25519 255 SHA256:lCMnSRLeXGJfKf7FgG8vGJoMQxJgiwHT3sNbzdHHR4E`

这只是网络端返回的指纹，尚未通过 Hetzner 控制台独立核对；尚未发送密码、建立 opsadmin、安装软件或迁移数据。接下来核对指纹，完成初始化和密钥登录验证，再检查容量、部署并验证新环境。旧实例保留用于迁移与回退。

## 关联文件与用途

- app/compose.loretide-dev.yaml：开发实例配置，本次显式声明附件目录。
- app/docs/development/remote-docker.md：说明附件宿主路径、持久化边界和本机素材区别。
- tasks/todo.md：当前实施状态，新增本次证据链接。
- 本记录：对话决策、实际验证与新机未完成事项；不含认证秘密。

## 构建容量结论修正

此前只验证一个页面的开发冷编译：约 4.51 GiB 峰值、159 秒；没有实测并行编译，也不能据此认定服务器只能验证流程或无法生产运行。开发工作可并行，重型构建暂按串行安排。
