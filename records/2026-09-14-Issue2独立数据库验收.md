# Issue2独立数据库验收推进

主任务于2026-09-14准备独立集群 C:\loretide-diag-acceptance-15402，复用既有 F:\loretide-runtime\pgsql\bin 二进制，仅127.0.0.1:15402监听。没有触碰15401或其他实例。

脚本关联：records/provision-diag-15402.ps1 是本次初始化脚本，随机生成独立秘密，ACL仅当前用户及SYSTEM，不可覆盖已有目标。秘密仅存本机受限目录，不纳入仓库或日志。脚本不含实际秘密。

验证身份 loretide_diag_acceptance_user，数据库loretide_diag_acceptance_local；superuser/createDB/createRole/replication/bypassRLS全部false。

在窗口2 PR3 HEAD 1d0dbd6037e931a0fcd91f66b540bcd0b6ac1d79 运行修订后的verify.ps1：8项命名Go测试精确run/pass；两项命名PostgreSQL集成测试通过（非SKIP）。当前CI 34771210226 success。PATH仅临时增加psql并在finally恢复。

只验证现有套件覆盖的事务/隔离/场景/导出服务层/保留期，不等同HTTP授权、流恢复或全部DG01完成。真实UI未自动验收，无UI改动，无新增手动Todo。独立集群继续保留供后续验收使用，不删除数据。
