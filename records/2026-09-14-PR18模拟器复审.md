# PR #18 主任务复审：需要补改

核对 head a10f26566789876b4544d7a21de53ff0da5f512e，范围为三个许可文件，当前 CI success。但 Issue #14 的核心断言仍不充分，暂不合并：

1. simulator_regression_test.go 的 ScenarioContracts 只检查 len(run.Events) < minEvents。timeout/cancel 等终止场景即使错误地继续产生后续事件也能通过。请按独立预期验证准确的组件序列、Sequence 和终止位置；先以 Fatal 检查长度，再索引，避免空数组 panic。
2. failingEvent.Outcome 接受 failed/cancelled/ignored 任意一个，无法区分取消与忽略。请逐场景指定准确 outcome，并检查 reconnect 的 attempt 转换位置及后续 attempt，而不只看最后事件。
3. DeterministicTimeAndIdentifiers 使用 Sleep(2ms) 并断言 Created 不相等。真实时钟不同不是契约；Issue 明确排除 Created 的逐字比较。删除等待和不相等要求，保留虚拟时间/Duration 确定性；补齐文档声称的跨 run Operation/Trace/Span 校验，或缩小文档宣称到实际覆盖。

仅在现有三个文件范围修订测试和记录，禁止修改生产代码，禁止 UI 单测及 computer use。用定向 Go 测试和 -race 验证，交付新 SHA 和 CI，保持 Draft。数据库持久化/事务验收仍归 #2。

## 补改复审通过
核对 head 62fbf34cbf8b2e5e1fa233e6c0d994a1be3fe445：精确序列/终止位置、outcome、attempt切换及ID比对已补齐，真实时钟依赖已移除。主任务在对应干净worktree重跑定向 TestSimulatorRegression -race -count=1，通过2.144s。当前CI 34770689534 success。PR #18已合并为13a0f50bd75c9ba1817356e46b2f700424de5a40，Issue #14关闭。无UI改动、无手动Todo，未操作运行服务或同步本地app。
