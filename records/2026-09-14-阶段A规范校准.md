# 阶段 A 权威规范校准记录

日期：2026-09-14。分支：`claude/docs-phase-a-calibration`（base `main` @ `7926540`）。执行者：Claude Code cloud 会话。

只改文档仓库 `main` 上的当前有效文档，不碰应用代码，不改 `tasks/*.md`（刚由 PR [#22](https://github.com/899ms/loretide/pull/22) 回写），不改写历史 records，不改需求正文与编号（R-/AC-/D11-/D13-/LT-/ARCH-/DIAG- 一律保留）。

## 为什么要改

文档里多处「实施状态」写于 2026-09-12～13，此后 app 侧合并了 PR #10、#12、#16～#19、#21、#23、#20，事实已经变了。留着「未创建实施工程」「尚未实现」会把已经交付、已有真机证据的东西也一并否掉，反过来又会让「已有代码」被误读成「已验收」。本次按四层分列：**已确认需求 / 已有实现 / 已验证 / 仍未验收**，每条都给可核实证据。

## 证据读取方式

app 侧证据用 `git show origin/chore/spec-kit-baseline:<path>` 读取，未切换 checkout。该分支在本次核查前已含 #19、#21、#23 三个合并（head `bdf1104`），并已由 PR #20 合入 `app-main`（head `3ecfaae`）。

> 过程记录：首次读取时本地 `origin/chore/spec-kit-baseline` 仍停在 `d721175`（三个 PR 合并之前），据此一度得出「`check:content-boundaries` 与 Acceptance mapping 不存在」的错误结论。重新 `git fetch` 后两者均存在。**读 app 证据前必须先 fetch 该 ref**，否则会把已交付的东西记成缺失。

## 逐条改动与证据

| # | 文件 · 位置 | 改动 | 证据 |
|---|---|---|---|
| 1 | `docs/11` 第 5 行 状态句 | 由一句「未创建实施工程，未完成应用功能或运行验收」改为三层：需求基线已确认；工程与原生 Windows 环境已建立且有验收记录；业务功能未实施、诊断与模块边界为「已有实现、部分验收」 | app `docs/development/native-windows.md`（当前开发在原生 Windows、无 Docker）；`native-env-acceptance-2026-09-13.md`、`native-env-reliability-2026-09-13.md`（**云端** Linux 实例的环境验收，非 Windows 本机，已在正文标明）；PR #19 / #21 / #23；`tasks/architecture.md`、`tasks/diagnostics.md` |
| 2 | `docs/11` 第 118 行 表格 | 「研究基准，未创建应用实施工程」→「研究基准；应用实施工程已从其派生（899ms/loretide `app-main`）」 | `origin/app-main` 存在，head `3ecfaae`（PR #20 合并提交） |
| 2b | `docs/11` 同节末段（原第 122 行） | 「实施代码、数据库、浏览器应用和真实 Agent 测试均尚未完成」→ 分述：代码与开发数据库已存在、浏览器可打开；审核规则合并与默认 Skill 加载未实施；真实 Agent 仍禁用且未做安全验收 | 见第 6 行各项证据。**此条不在下达的逐句清单内**：它与第 1、2 条同属「当前源码与交付状态」节，不改就与同节新内容自相矛盾。已单列，供主任务裁决 |
| 2c | `docs/11` 第 84 行 | 「应用任务状态仅在 todo 维护，当前全部待实施」→「…逐项状态以该文件为准（已非全部待实施）」 | `tasks/todo.md` 第 5 行：LT-001～002、CP-01 已完成，LT-003/005/006/007 进行中。**此条不在逐句清单内**，与同文件新状态句直接冲突，一并更正并单列 |
| 3 | `docs/11` 模块约束段之后 | 新增 UI 复用规则：新增/修改的 Web 页面必须完整复用 Multica 组件、布局、排版并继承颜色与字号 token，禁止手写新页面控件或布局；诊断页与内部工具页同样适用 | 主任务确认的规则；与 app `CLAUDE.md` → UI Rules 一致 |
| 3b | `docs/11` 同段 | 「自动检查尚未实现」→ 已实现并接入 CI，附 ARCH-01/02 证据指针 | app `scripts/check-content-boundaries.mjs`、`scripts/content-boundaries.json`、`.github/workflows/loretide-content.yml`、`docs/development/content-boundary-checks.md` → `## Acceptance mapping (ARCH-01/02)`；PR #19 |
| 4 | `docs/11` 第 130 行、`docs/04` 第 265 行 | 需求句原样保留；句末「当前仅规格与任务更新，尚未实现。」→「当前状态见 docs/13 首行与 tasks/diagnostics.md。」 | 不在此处重复状态，避免三处各写一份又各自过期 |
| 5 | `docs/13` 第 5 行、第 81 行 | 首行改为「均为必做 + 当前状态」两段，明确「不能以已有代码视为验收通过」；§9 标题「（全部待执行）」→「（部分自动化已通过，浏览器矩阵待执行）」 | app `server/internal/content/diagnostics/`（15 个文件，含 4 个 `*_regression_test.go`）；`docs/development/diagnostics-{log,simulator,snapshot}-regression.md`、`ci-diagnostics.md`；PR #16 / #17 / #18 / #21；app `specs/002-diag-package-stream-recovery/manual-ui-todo.md`（37 条手动项，全部「待用户验证」） |
| 6 | `README.zh-CN.md` / `README.md` 状态表 | 见下一节逐行 | 见下一节 |
| 7 | `CONTRIBUTING.md` 开头、第 24 / 37 行 | 声明本文件为协作规则权威、app 侧 `ai-collaboration.md` 为摘要并注明对应 SHA；两处「浏览器证据」加限定「由用户手动验证产生；禁止 computer use / 自动点击验收」 | 逐字 diff：app 的 `docs/development/ai-collaboration.md` 与本文件 `6576846` 版本**只差第 3 行**（引言被改写），其余完全相同；本文件此后前进到 `eb07ba2`（新增 GitHub 预占段落），摘要未同步 |
| 8 | 本文件 | 新增 | — |

### README 状态表逐行

| 原行 | 新状态 | 证据 |
|---|---|---|
| 正式应用工程与数据库：尚未建立 | **已建立** | `app-main` 分支；app `docs/development/native-windows.md`（原生 Windows、无 Docker、本机 PostgreSQL） |
| 审核能力合并：尚未实现 | **仍为尚未实现** | `app-main` 上未见审核规则合并产物，仅 vendor 原始快照。无证据，按要求保持原状 |
| 默认 Skill 加载：尚未实现 | **仍为尚未实现** | app `server/internal/service/builtin_skills/` 只有 `multica-onboarding/`、`multica-platform/` 两项上游自带内容 |
| Go 门禁：尚未实现 | **已实现** | `executionpolicy.Check()` 恒返回 `ErrDisabled`；app `docs/development/execution-gate-tests.md`；PR #10；`ci-diagnostics.md` 列为 CI 检查项。已注明**这不等于真实执行器安全验收** |
| 浏览器应用：尚未执行 | **已可运行** | app `scripts/local-windows.ps1`；`native-windows.md` → Update log 2026-09-14 第三行：主任务在 Windows 11 / pwsh 7.6.6 真机跑出 `status -Json` api 与 web health 200、冷态 start 14s、stop 后 PostgreSQL 仍在；PR #23 |
| 真实 Agent：尚未执行 | **仍禁用、未验收** | 同 Go 门禁一行；执行策略恒为 disabled |
| 远程恢复验收：尚未执行 | **未验收** | app `native-env-reliability-2026-09-13.md` 是**云端实例的环境恢复**记录，不是业务验收 |
| 开发工作包：全部待实施 | **分列** | LT-001～002 已完成（`tasks/todo.md` 证据链接）；ARCH-01/02（#19）、LT-008（#23）、DIAG-08/12（#21）已交付待验收；其余待实施 |
| 尾段「下一可执行项是 LT-001」 | 改为指向 `tasks/todo.md` / `tasks/diagnostics.md` | LT-001 状态为 DONE。**此条不在逐句清单内**，与同段状态表直接冲突，一并更正并单列 |

## 仍未验收（本次不改其状态）

- **D13-V01～V12 浏览器验收矩阵**：未逐项通过。app `specs/002-diag-package-stream-recovery/manual-ui-todo.md` 的 37 条手动项全部「待用户验证」，其中 V11-10 / V11-11 为破坏性操作，只能在一次性本地实例上执行。
- **DG-01**：未整体通过（`tasks/diagnostics.md` 明载）。LT-009 仍被它阻塞。
- **ARCH-01/02、DIAG-08/12、LT-008**：卡片为「已交付/部分交付，待合并验收」。PR 现已合并，但用户验收未完成，本次**不勾选**任何任务卡（constitution 原则 X：勾选 ≠ 验收）。
- **`tasks/todo.md` 的 LT-008 仍记 TODO**：PR #23 已交付并真机验证，卡片未回写。本次按「不改 tasks/*.md」未动，留给主任务。
- **真实执行器安全验收**：未做。门禁测试只覆盖静态/接口行为，不证明运行时权限隔离（app `execution-gate-tests.md` 自述）。
- **审核能力合并、默认 Skill 加载**：无证据，保持「尚未实现」。
- **远程恢复业务验收**、**真实 Agent 验收**：未执行。
- **`docs/12` §2 与 `content-boundaries.json` 的逐名比对**：app 侧 Acceptance mapping 记载主任务已于 2026-09-14 逐名核对（11 + `diagnostics`，差异 0）。本次未重跑该比对，按记载引用。

## UI 影响

无。本次只改 Markdown 文档，无页面、无代码。

## 手动 UI Todo

无。

## 回滚

`git revert` 本分支的提交，或关闭 PR 不合并。无代码、无配置、无迁移，回滚无残留。
