主任务裁定：同意 #99 最小扩界，新增 diagnostics Store.AuditTx 及专属非UI测试，允许修改 server/internal/content/diagnostics/store.go、新增 audit_tx_test.go（如真实DB测试需另命名可登记）、022合同/计划/记录。已核对现有 Audit 自建事务，不能满足原子性。必须保留 scope/workspace/account/actor 校验与 Sanitize，并在传入事务中执行现有 guard.LockForContentDiagnosticWrite，不能绕过工作区删除锁；锁顺序要与topic写事务一致，必要时在业务写前调用/获得同一guard并说明。禁止自行Begin/Commit/Rollback调用方事务，nil依赖失败封闭。topic模块禁止直接写diagnostics表，调用方必须传播审计错误并回滚业务事务。验收至少覆盖同事务成功、审计失败业务回滚、调用方回滚审计消失、越权拒绝且无副作用；真实DB只用独立合成测试库。原Audit行为保持兼容。不扩展诊断枚举等其他范围，额外问题另报。请继续实施并把扩界单列PR说明。
