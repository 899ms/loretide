主任务验收结论：保持 Draft，暂不合并。经执行者只读核对当前 CI job 105905245267，现有 workflow 未执行本任务新增 ip-profile、handler 与真实路由测试。CI green 不能作为这些功能已验证的依据。

已有证据：本地 ip-profile 包 PASS=74/SKIP=0/FAIL=0（执行者报告）。缺口：5项新增handler测试及1项真实路由测试仅 go test -c 编译，未运行，不能标作通过或skip。受影响的既有路由表/auth测试同样未运行。

下一验收门槛：先完成独立数据库测试入口失败封闭方案审查，再在明确隔离的临时测试数据库实际执行新增handler/路由用例，并提供逐项结果；禁止fallback到localhost:5432/multica，禁止触及用户业务库。本条不授权重新运行或改CI，环境方案另行审查。
