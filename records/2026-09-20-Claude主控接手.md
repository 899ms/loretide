# Claude 主控接手 · 2026-09-20

接手依据：`records/2026-09-20-Claude主控交接.md`。本文件记录接手时的核对结果、保留的限制与首批指令。

## 通道

Claude 端**没有** Codex 线程连接器（可用的只有 Claude 会话互发与 GitHub 工具）。按交接文件约定：不宣称直接接管 Codex 对话；主控指令写入本仓库 `records/` 与 GitHub Issue/PR 评论，由用户在 Codex 主任务转发。Codex 主任务只作中继。

## 接手时核对（只读，未连库、未跑测试、未动服务）

- app-main 仍为 `5d6d47f29`（LT-016，#93），暂停期间未变。
- 窗口 1：`codex/issue-98-account-expression-profile` @ `e8ff309fb`，建立在 Claude 021 WIP（`d1b9d3df6`）之上；PR #100 Draft/Open，`MERGEABLE`，两项 CI SUCCESS（`boundaries`、`issue-98-account-profile-db`）。工作树干净。
- 窗口 2：`task/issue-99-topic-brief-backend` **未推送**；静态差异完成，未提交、未测试；有两项超范围 diff（`loretide-content.yml`、`scripts/test-local-windows.ps1`）；9-19 曾 6 次触发 `TestMain` 回退连到 `localhost:5432/multica`，影响未知。
- 两个 Claude 云会话（016nc、01VRK）已被 Codex 接手的工作取代：`claude/spec-021-…` 是 codex 分支的祖先；`claude/spec-022-…` 的规格 PR #96 对应 #99。

## 保留的限制（原样沿用）

1. UI 由用户人工检测；禁止 UI 单元测试与 computer use 验收；开发中不主动处理 UI 缺陷，只提供受影响 UI 与手验 Todo。
2. 真实 AI 执行器保持禁用。
3. 本地 DB 连接、测试、迁移、服务操作禁止；不为调查事故再次连接、清理或恢复数据库。
4. 窗口 2 提交/推送/触发 CI 未批准；窗口 1 仅有 PR100 特定 GitHub-hosted 隔离 CI 权限。
5. 新任务先 Issue 预占、独立 worktree、Draft PR、主控审查；两项超范围 diff 不得未审混入。

## 与 Claude 主控自身的已有规则叠加

`docs/development/spec-kit-workflow.md` 第 12 步（路由存在性 + 参数≠上下文用例）、第 13 步（上游改动单列 `upstream:` 提交）、SOP 阶段 UI 规则（整套继承上游设计系统）、`.claude/skills/go-modern-guidelines` 的范围限制，对 Codex 窗口同样生效。

## 首批指令见 GitHub

- Issue #98 / PR #100 评论：PR100 完整审查结论与合并条件。
- Issue #99 评论：fail-closed 提案与超范围 diff 的裁决、推送/CI 授权范围。
