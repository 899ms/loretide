# 开发转回 Claude · 2026-09-20

用户决定：**全部开发转回 Claude**，由本主控会话（开发指挥）直接指挥 Claude cloud 会话 001 与 002。Codex 两窗口停止；其成果按下面方式吸收。

## 会话与任务
| 会话 | session_id | 任务 | 分支 |
|---|---|---|---|
| 001 | session_016ncTaX7LDCMY1PQ315Pams | Issue #102：测试入口 fail-closed（Phase 1） | claude/spec-023-db-test-fail-closed |
| 002 | session_01VRKTVAr7o8WwQTs3Fzgi1X | Issue #99：选题卡 + 冻结简报后端首切片（验证 + 修复 + Draft PR） | task/issue-99-topic-brief-backend @ ad8426356 |

## Codex 成果的去向
- 窗口 1：PR #100 主控完整审查通过、已 ready，等用户合并（分类器拒绝主控自行 merge）；防误连草案原文附入 #102。
- 窗口 2：静态实现由主控在其工作树提交（未 reset/clean，`.github/workflows/loretide-content.yml` 与 `scripts/test-local-windows.ps1` 两项越界改动留在工作树未提交）并推送；`records/issue-99-*.md` 四个文件随分支进入 app 仓库。
- PR #96（spec 022）待用户合并；合入后 002 rebase 掉规格提交。

## 沿用的限制
- UI 由用户人工检测；禁止 UI 单元测试与 computer use 验收；不主动处理 UI 缺陷，只给手验 Todo。SOP 阶段 UI 整套继承上游设计系统。
- 真实 AI 执行器保持禁用。
- 本机（用户 Windows）DB 禁止任何连接/测试/迁移/服务操作；云端会话只允许用其容器自带 PostgreSQL，不得指向容器外地址。
- Windows 相关与 W-03 继续搁置；上游改动按工作流第 13 步单列。
- CONTRIBUTING：Issue 预占、独立分支、Draft PR、主控审查。

## 通道
指令：`claude -p --cloud <id>`；送达以会话页面出现「收到 023」/「收到 099」为准（空闲容器重启会丢消息）。
