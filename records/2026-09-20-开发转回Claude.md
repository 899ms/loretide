# 开发转回 Claude · 2026-09-20

用户决定：**全部开发转回 Claude**，由本主控会话（开发指挥）直接指挥 Claude cloud 会话 001 与 002。Codex 两窗口停止；其成果按下面方式吸收。

## 会话与任务
| 会话 | session_id | 任务 | 分支 |
|---|---|---|---|
| 001 | session_016ncTaX7LDCMY1PQ315Pams | Issue #102：测试入口 fail-closed（Phase 1） | claude/spec-023-db-test-fail-closed |
| 002 | session_01VRKTVAr7o8WwQTs3Fzgi1X | Issue #99：选题卡 + 冻结简报后端首切片（验证 + 修复 + Draft PR） | task/issue-99-topic-brief-backend @ ad8426356 |

## Codex 成果的去向
- 窗口 1：PR #100 主控完整审查通过、已 ready，等用户合并（分类器拒绝主控自行 merge）；防误连草案原文附入 #102。
- 窗口 2：静态实现由主控在其工作树提交（未 reset/clean，`.github/workflows/loretide-content.yml` 与 `scripts/test-local-windows.ps1` 两项越界改动留在工作树未提交）并推送；`records/issue-99-*.md` 四个文件随分支进入 app 仓库。
- PR #96（spec 022）待用户合并；合入后 002 rebase 掉规格提交。

## 沿用的限制
- UI 由用户人工检测；禁止 UI 单元测试与 computer use 验收；不主动处理 UI 缺陷，只给手验 Todo。SOP 阶段 UI 整套继承上游设计系统。
- 真实 AI 执行器保持禁用。
- 本机（用户 Windows）DB 禁止任何连接/测试/迁移/服务操作；云端会话只允许用其容器自带 PostgreSQL，不得指向容器外地址。
- Windows 相关与 W-03 继续搁置；上游改动按工作流第 13 步单列。
- CONTRIBUTING：Issue 预占、独立分支、Draft PR、主控审查。

## 通道
指令：`claude -p --cloud <id>`；送达以会话页面出现「收到 023」/「收到 099」为准（空闲容器重启会丢消息）。

## 更新 · PR #100 P1（同日）
- Codex 主任务已合并 #96（9d468905c，分支保留）；#100 因自动审查 P1 未合并，记录见 `records/2026-09-20-PR96与PR100合并执行.md`。
- 主控复核：P1 成立但属 ip-profile 模块级缺口（#71/#73 起全部写路径均未取 `LockWorkspaceForContentDiagnosticWrite`），非 #100 引入。裁决：#100 内先修 `SetPersonaPrompt`/`SetProfile` 两条（会话 001 插队执行），模块其余写路径与 topic-planning 自查转 Issue #104；两条 P2（未知字段结转、core parity）不阻塞，记入 #104。
- 会话 002 已通知 rebase 到 app-main 并自查 #99 的栅栏。

## 更新 · 当日合并与新派发（晚间）
- #107（#99 EP-04a PR 1，会话 002）主控审查通过 → 用户经 Codex 合入 app-main `d9fe4f3f3`。合入后自动审查 P1（AppendBrief 版本号子查询与行锁同语句）复核成立 → Issue #109，派会话 002。
- #100（#98 021 存储与接口，Codex 起点 + 会话 001 修 P1）主控放行 → 合入 app-main `af0c77d6`。整改要求：上游 `issue_table_query.go` 纯 gofmt 改动去掉；`content_diagnostics.go` gofmt 展开拆独立 style 提交。
- 新 Issue：#104（ip-profile 其余写路径栅栏，派 002，在 #109 后）、#108（脱敏白名单缺内容模块名，待裁决）、#109。
- 用户本机 app 检出已快进到 `af0c77d69`。本机开发库停在迁移 481，树已到 489；按规则主控不碰本机 DB，用户需要本地起服务前自行 `go run ./cmd/migrate up`。
- #98、#99 由用户/Codex 关闭。

## 更新 · 晚间第二批
- 会话 002：#110（#109 修复）审查通过、ready；#113（#104 剩余 ip-profile 写路径）Draft，静态复核通过、等 CI。
- 会话 001：#112（#102 Phase 1 fail-closed，含 `upstream:` 独立提交改 `ci.yml` 17 行纯增量）Draft，静态复核通过、等 CI。
- 新 Issue：#114（Phase 2：346 文件 dbtest 分类）、#115（上游用例 TestCommentSourceContextLifecycle 在 app-main 既有失败）。
- Codex 窗口 1 的两份只读产物搬入本仓库：`records/2026-09-19-Codex-DB测试文件只读分类清单.md`、`records/2026-09-19-Codex-DB测试防误连方案草案.md`（#114 的输入）。
- #98、#99 已由主控关闭。代码 PR 的合并仍被权限分类器拦截（自我审批），需用户加 `Bash(gh pr merge:*)` 允许规则或由用户/Codex 代点。

## 更新 · 夜间：权限放开后的合并与新派发
- 用户把会话权限改为 acceptEdits，主控自行合并：#110 → `a14834978`（关 #109）、#113 → `58763aed7`（关 #104）、#112 → `54c715ec3`（关 #102；CI 三作业全绿，db-suites handler 3801 PASS / cmd-server 489 PASS）。用户 app 检出已快进到 `54c715ec3`。
- app-main 现状：迁移到 489；两个 DB 套件 fail-closed，运行方式见 `docs/development/testing-database-suites.md`；通用 CI 作业 `db-suites`。
- 新派发：#117（021 页面 PR + core 就绪判定 parity）→ 会话 001，分支 `claude/impl-021-profile-page`；#118（022 页面 PR 2）→ 会话 002，分支 `claude/impl-022-topic-brief-page`。两者都按 SOP 阶段 UI 规则：只挂既有组件、不写 UI 单测、界面项进 manual-ui-todo 供用户手验。
- 待派/待裁决：#114（Phase 2 分类）、#115（上游用例既有失败，#112 合入后重跑判断）、#108（脱敏白名单）。

## 更新 · 深夜：两个页面 PR 合入，SOP §3.1 与 §5.2/5.3 首次可在 web 上操作
- #120（021 页面 + core 就绪判定 parity，会话 001）→ app-main `4bcb49459`，关 #117；16 条手验项在 `specs/021-account-expression-profile/manual-ui-todo.md`。
- #119（022 页面 PR 2，会话 002）→ app-main `685a3dec5`，关 #118；14 条手验项在 `specs/022-ep04-topic-brief/manual-ui-todo.md`。页面 `/{slug}/accounts`、`/{slug}/topics`，无侧栏入口，URL 直达。
- 新发现：`cmd/migrate` 的上游用例 `TestEveryConcurrentUpBuildHasCleanup` 在 app-main 上 FAIL，自 472 起 12 条 content 索引迁移都没登记 CONCURRENTLY 失败重试清理 → Issue #122，派会话 001。
- 会话 002 → #115 复核（上游用例同库二跑失败的残留来源）。
- 用户手验前置：本机开发库 481→489 迁移（备份 → `go run ./cmd/migrate up` → 重复执行验证 → 更新 native-windows.md）。

## 更新 · 2026-09-21 凌晨
- #124（#122 迁移清理登记，会话 001）→ app-main `24ee6caec`，关 #122。实际 15 条（469/470/471 也漏登记，主控本机 `tail` 截断了输出）；上游 `cmd/migrate/main.go` 追加 21 行单列 upstream 提交；R6 约束测试；`loretide-content.yml` 新增迁移策略纯文件检查步骤（app-main 此前从不跑 `cmd/migrate`）。
- #115 定位结论（会话 002）：上游 `ClaimSourceContextObjectIntentForCleanup` 真实缺陷（LIMIT 1 子查询在 Nested Loop 内层时一条语句认领全部到期行，sqlc :one 只读首行），生产清理吞吐同样受影响。按「来自上游的先不处理」本仓库不修，Issue 改题 [上游缺陷·搁置] 保持 open；会话 002 整理上游报告材料供用户决定是否提交 multica-ai/multica。
- #108 裁决：白名单以 `content-boundaries.json` 登记为唯一来源；派会话 001。
- 待：#114 Phase 2；用户 30 条手验。

## 更新 · 2026-09-21：#126 合入；#114 撤销（主控误判）；新派 #127、#130
- #126（#108 白名单，会话 001）→ app-main `ff842e3d6`，关 #108。
- **#114 撤销**：会话 002 交出 PR #128（handler 296 文件按 dbtest 分类，判定可审计、两模式均过），主控复核后不合并——#112 的 `db-suites` 已整包跑全部 3808 条，分类没有覆盖收益，却改 297 个上游测试文件（57 个拆分），违反「上游只在 SOP 需要时最小改动」。责任在主控立项时未算收益/代价。教训：涉及上游文件的基础设施任务，立项前先算「合入后上游同步的冲突面」。
- 新派：#127（EP-04b 开始界面与输入快照规格，会话 001，分支已推）；#130（022 补：选题卡关联账号，会话 002）。
- #115 上游缺陷材料齐全，待用户决定是否提交 multica-ai/multica。

## 更新 · 2026-09-21：#129 规格合入、#133 合入
- spec 023（EP-04b 开始界面与输入快照）裁决：Q1=B（新表 `content_start_snapshot`，append-only；否决推荐值 A 因其受限 UPDATE 打破 022 简报只插不改守卫）、Q2=A（九个无来源字段恒空各一条负例）、Q3=A（资料范围三分记法与偏好写回）、入口=选题卡详情页 start 之后。修订后 #129 → app-main `d2aa30990`，关 #127；实施 PR 1 → Issue #132，会话 001。
- #133（#130 选题卡关联账号，会话 002）→ app-main `6ff5452a5`，关 #130；手验项 022-U-15～22 追加，用户手验总数 38 条。
- 会话 002 待 #132 合入后接 023 页面 PR。

## 更新 · 2026-09-21：#135 合入，EP-04b 存储与接口落地；新派 #136、#137
- #135（023 PR 1，会话 001）→ app-main `6085983fa`，关 #132。迁移 490–493（新表 `content_start_snapshot`，三索引并发 + 登记清理），三个端点 `POST …/briefs/{revisionId}/start`、`GET …/briefs/{revisionId}/snapshots`、`GET …/snapshots/{snapshotId}`；与 #133 冲突后 rebase 一次再合。
- 新派：#136（023 页面 PR，会话 002，分支 `claude/impl-023-start-page`）；#137（024 规格：work-editor 首切片，人工写作的作品容器与版本，SOP §6.2/§7，会话 001，分支 `claude/spec-024-work-editor-manual`）。
- 用户本机开发库需迁到 493（此前记录写 489 已过时）。

## 更新 · 2026-09-21：#139 规格与 #140 页面合入；EP-04b 全部落地
- #139（024 work-editor 规格，会话 001）→ app-main `4b7e6ffcc`，关 #137。裁决：Q1=A（编辑副本为文档行可变列、版本独立 append-only；不自动存版；补 workspace_id 打头索引）、Q2=A（只存 id 字符串、不 import topic-planning）、Q3 严格按 SOP §7.1 原文（编辑副本仅 working/saved；版本 source=generated/edited/adopted 三值；恢复/采用为 action 列并记来源版本）。实施 PR 1 → #141，会话 001。
- #140（023 页面 PR，会话 002）→ app-main `64812f8fe`，关 #136。EP-04b（开始界面与输入快照）存储、接口、页面全部落地；手验 023-U-01～21，用户手验累计 59 条。
- 会话 002 下一项：#（025 review-delivery 规格，人工审核/交付/发布记录，SOP §8–9.2）。

## 更新 · 2026-09-21：#145 合入（work-editor 存储与接口）；025 裁决
- #145（024 PR 1，会话 001）→ app-main `b567f8cbe`，关 #141。迁移 494–503（`content_work` / `content_artifact` / `content_artifact_version`，七索引并发 + 登记），十二条端点，落地模块 4→5。会话 001 接页面 PR #146。本机开发库迁移目标现为 503。
- spec 025（review-delivery 首切片，会话 002，PR #144）裁决：Q1=A（审核请求行 `snapshot jsonb`，字段按 §8 定死，attachments 恒空）、Q2=A + §9.3 held 规则、Q3=A（迁移记录表，held/取消/失败/延后必带 reason）、Q4=A（渠道四个受控值、交接三种且不等于发布成功、只允许渠道稿提交审核）、Q5 不发明枚举（actor + 自由文本、version_match 三态）。等回写后合并。

## 更新 · 2026-09-21：#144 规格合入；#146、#148 派发
- #144（025 review-delivery 规格，会话 002）→ app-main `664a01190`，关 #142；五条裁决与 SOP §8/§9 原文已回写。实施 PR 1 → Issue #148，会话 002，分支 `claude/impl-025-review-delivery-api`。
- 会话 001 在做 #146（024 页面 PR）。
- SOP 落地进度：§3.1 ✓、§5.2/5.3 ✓、§5.2 账号关联 ✓、EP-04b 开始与快照 ✓、§7 作品与版本（接口 ✓，页面进行中）、§8–9.2 审核/交付/发布记录（规格 ✓，接口进行中）。

## 更新 · 2026-09-21：#150 合入（review-delivery 存储与接口）；#152 待 CI；新派 #154、#155
- #150（025 PR 1，会话 002）→ app-main `d70dbc17c`，关 #148。迁移 504–515（`content_review_request` / `content_review_transition` / `content_delivery_task` / `content_publication_record`，八索引并发 + 登记），九条端点，落地模块 5→6。整改一次：上游 `cmd/migrate/main.go` 登记改为独立块，既有行零删除。本机开发库迁移目标现为 515。
- #152（024 页面 PR，会话 001）审查通过，等 CI 自动合并：选题卡页面开插槽、web 适配器组合 work-editor，不改登记表；27 条手验项。
- 新派：#154（025 页面 PR，会话 002）；#155（026 今日工作台规格，会话 001，#152 合入后）。新立搁置 Issue：#151（pkg/agent Codex 用例）、#153（onboarding 用例）。
- 用户手验累计 86 条（021:16、022:22、023:21、024:27）。

## 更新 · 2026-09-21：#152 合入，SOP §7 作品与版本全部落地
- #152（024 页面 PR，会话 001）→ app-main `9f1c75ad4`，关 #146。架构取舍：不改登记表，选题卡页面开插槽、web 适配器组合 work-editor。
- 派发：#155（026 今日工作台规格）→ 会话 001；#154（025 页面）→ 会话 002（rebase 到含 #152 的 app-main，「提交审核」入口用插槽注入）。
- SOP 落地进度：§3.1 ✓、§5.2/5.3 ✓、EP-04b ✓、§7 ✓（接口 + 页面）、§8–9.2（接口 ✓，页面进行中）、§2 今日工作台（规格进行中）。

## 更新 · 2026-09-21：#158 今日工作台规格合入；#159 派发
- #158（026 规格，会话 001）→ app-main `aa50ce711`，关 #155。裁决：Q1=B（不新建模块，已有读端点由适配器组合，派生口径进 core 纯函数；将来服务端聚合归 project-collab）、Q2=A（选题区块只列 draft；EP-04c 落地须带来源字段）、Q3=C（`/{slug}/today`，默认落地页另议）；区块按 §2 原文五项（第五项「待补录的反馈」暂不可用）+ 第六附加「账号配置缺项」。主控此前把 §2 第五项误转述为「待补充的经营底座」，已按原文更正。
- 实施 → Issue #159，会话 001。会话 002 仍在做 #154。

## 更新 · 2026-09-21：#161 合入，SOP §8–§9.3 人工切片全部落地
- #161（025 页面 PR，会话 002）→ app-main `c2d0edecb`，关 #154。入口经 work-editor 插槽由适配器注入，登记表未改；42 条手验项，用户手验累计 128 条（021:16、022:22、023:21、024:27、025:42）。
- 进行中：#159（今日工作台页面，会话 001）；#162（027 feedback-learning 规格，会话 002，分支已推）。
- SOP 落地进度：§2 今日工作台（页面进行中）、§3.1 ✓、§5.2/5.3 ✓、EP-04b ✓、§7 ✓、§8–9.3 ✓、§10.1（规格进行中）。依赖执行器的环节（§6 Agent 工作流、§10.2 AI 复盘、候选选题生成）继续禁用。

## 更新 · 2026-09-21：#163 规格合入；#165 待 CI；#166、#167 派发
- #163（027 feedback-learning 规格，会话 002）→ app-main `251001e3b`，关 #162。裁决：指标记录十列按 §10.1 原文、指标名受控恰十一项、空≠0、只插不改、CSV 首版为粘贴文本；Q2=A（绑发布记录，版本两跳）；复盘占位状态集取 §7.1 全集仅产生 pending_data；摘录来源受控三项、摘录与解释分列；待补录派生无时间逻辑；实施同时更新 026 FR-004/005b。实施 PR 1 → #166，会话 002。
- #165（026 今日工作台页面，会话 001）审查通过，等 CI 自动合并：派生口径落在 `packages/core/today/`（content/ 之外、不 import 内容模块），登记表仅加 adapter 一条；审核/交付区块已接 #161；27 条手验。发现 021 的 profile hook 吞错 → #167，派会话 001。
- 用户手验累计 155 条（021:16、022:22、023:21、024:27、025:42、026:27）。

## 更新 · 2026-09-21：#165 今日工作台合入；SOP §2 入口页落地
- #165（026 页面，会话 001）→ app-main `fae5dce04`，关 #159。`/{slug}/today` 六个区块只读聚合，未设默认落地页、未加侧栏。
- 会话 001 随即交出 #169（#167 修复：profile hook 不再吞错），待主控审查；会话 002 在做 #166（027 存储与接口）。
- 主控会话中途重启一次，后台等待任务丢失，已按 GitHub 现状重建。

## 更新 · 2026-09-21：#169 合入；028 source-inbox 规格裁决
- #169（#167 profile hook 不再吞错 + 今日工作台同款修复，会话 001）→ app-main `8d24cad47`，关 #167。失败态不再渲染可编辑表单（原先会把空档案确认成一版）。
- spec 028（source-inbox 首切片，会话 001，PR #172）裁决：Source 字段按 §4 步骤 1/4 与 PRD R-009/010（kind 受控 pasted_text/url、historical_import 布尔创建时定、整理状态 inbox/organized/archived）；本卡建 SourceSnapshot（粘贴原文 + sha256），URL 首版 0 抓取；去重按哈希只提示；Q2=A（不做选题卡引用）；Q3=A 修正版（不建解析状态列，随解析器卡出现）；不接今日工作台。等回写后合并。
- 会话 002 仍在做 #166（027 存储与接口）。
