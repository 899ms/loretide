你现在接手 loretide 项目的开发主控。工作目录 F:\GJ\内容创作工作台 是文档仓库（分支 main）；应用仓库嵌套在 ./app（独立 .git，分支 app-main，Go + pnpm monorepo）。两个仓库的历史永不合并；操作应用仓库一律 cd app 或 git -C app；GitHub 远端 https://github.com/899ms/loretide，gh 始终显式 --repo 899ms/loretide。

先读，再动手：
1. records/2026-09-21-Claude停止交接Codex.md —— 停止时状态、开放 PR/Issue、限制清单（全部原样沿用）
2. records/2026-09-20-开发转回Claude.md —— 两天的逐条记录与全部裁决
3. app/docs/development/spec-kit-workflow.md、app/CONTRIBUTING.md、app/.specify/memory/constitution.md
4. app/specs/030-topic-source-refs/ 与 app/specs/031-historical-import/（已合入的规格，待实施）

接手后按顺序做：
A. 审查并合并 PR #216（Issue #208）：核对 CI 三项（boundaries、db-suites handler、db-suites cmd-server）全绿、workspace-tab.test.tsx 零改动、upstream: 提交单列；squash 合并、关闭 #208。
B. 审查 PR #217（Issue #214）：对照 specs/031 的 spec/contract/tasks 与裁决；本机跑 go vet、go build ./cmd/server、go test ./cmd/migrate ./internal/migrations、三项 check、core vitest、触及的 packages/views/<dir> vitest、rich-content/package-exports.test.ts；迁移文件排除注释后不得含 PRIMARY KEY/UNIQUE/REFERENCES/CASCADE；CI 绿后 squash 合并、关闭 #214。
C. 派发并实施 031 PR 2（页面）与 Issue #211（030 实施，可拆存储与接口/页面）。执行者可以是 Claude 云会话 001/002（claude -p "<msg>" --cloud <session_id> --output-format json < /dev/null；ok:true 不等于送达，要在 claude.ai/code 会话页确认；规格 PR 合并后必须主动通知执行者）或你自己，按 CONTRIBUTING 走 Issue → Draft PR → 审查 → squash。
D. 每次合并或改系统状态后，在 records/ 追加记录（日期/做了什么/证据），走 PR 合入 main，并让本机文档主检出快进。

绝对不做：连接用户本机数据库；写 UI 单测或 computer use 验收；主动改 UI 样式；开启真实执行器；碰 Windows/W-03/文件输入；为风格改上游文件；改登记表 modules 依赖；合并未跑过本机核对与 CI 的 PR。

用户的两项待办与你无关，不要代做：本机开发库迁移 481→531；232+ 条手验（各 app/specs/*/manual-ui-todo.md）。
