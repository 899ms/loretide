# 派单恢复与停滞排查 · 2026-09-25

用户指示「继续安排开发任务」。本次由 Claude 开发指挥会话排查停滞并重新派单。

## 停滞原因（证据）
- app-main 自 2026-09-22 07:51 UTC（`cea2bb43b`，#243）后无新合入；CI 自 09-22 07:17 起恢复全绿（Actions 额度问题已解除）。
- Multica 本机 daemon 正常（profile `desktop-mu.chengmutian.com`，v0.5.1，已运行约 69 小时，心跳正常）；四个智能体全部 idle。
- 看板 GLN-9/10/11 状态过期：对应 PR #228、#227、#229 已于 09-22 合入。
- 主控 09-22 07:52 把 #244、#245 预占给 **Codex 桌面窗口 1、2**（非 Multica 智能体）。两个 worktree 最后修改分别在 09-22 16:01 / 16:02，之后无活动，Codex 桌面进程未运行。未提交改动：
  - `.agents/worktrees/topic-body-edit-ui`（#244）：9 文件，约 +220 行。
  - `.agents/worktrees/diagnostic-component-facts-a2`（#245）：7 文件，约 +399/-43 行。
- 结论：队列里没有可被 Multica 调度的任务，派出去的两张卡落在已停的 Codex 窗口上，无人发现。

## 本次动作
- GitHub 关闭已由合入 PR 完成的 Issue：#235（#236）、#220（#228、#231）、#211（#227、#229）；界面手验项仍按各自 `specs/*/manual-ui-todo.md` 由用户执行。
- Multica 看板 GLN-9、GLN-10、GLN-11 标记 done。
- 新建 Multica 任务，挂项目「工作台app目录」（`F:\GJ\内容创作工作台\app`），**在原 worktree 原地续写、不重做**：
  - GLN-12 → codex sol开发：#244 选题详情五项正文编辑页面。
  - GLN-13 → Claude-F5.1：#245 诊断 A2 宿主配置事实接入。
- #244、#245 各留改派评论，说明原 Codex 窗口若恢复不得再动这两个 worktree。
- 未处理：`F:\GJ\内容创作工作台\.multica\daemon_task_context.json`（GLN-8 in_place 运行标记）保留不动；Multica CLI 需从该目录外执行。

## 后续
- 两个 Draft PR 交付后由主控审查合并（审查清单见 `records/2026-09-21-Claude停止交接Codex.md` 第 9 条）。
- 待派积压：无（Web SOP 与诊断审计 #232/#233 列出的实现缺口，在 #244、#245 后清零）。

## 更正（同日）：主控回到本地「开发指挥」会话，执行者为两个 Claude 云会话
用户明确：由当前「开发指挥」窗口担任主控，任务派给 Claude 云会话「Spec 001」「002」，不经 Multica。
- Multica GLN-12、GLN-13 已取消。取消前 Claude-F5.1 已完成 #245 并开出 PR #247（`codex/diagnostic-component-facts-a2` @ `a40ce99b9`），保留不重做。
- #244 的 Codex 半成品原只在本机 worktree，已提交推送到 `codex/topic-body-edit-ui`（`89b66d120` 代码 + `319ad9048` 任务记录），供云会话读取。
- 派发（两个会话均已在会话页确认「收到」）：
  - 会话 001（`session_016ncTaX7LDCMY1PQ315Pams`）：#244 在 WIP 分支上补完并开 Draft PR。
  - 会话 002（`session_01VRKTVAr7o8WwQTs3Fzgi1X`）：PR #247 第二审查，只报告不改代码。
- 两个会话页均显示「Approaching weekly usage limit，Resets Sat, Sep 26, 10:00 PM」。

## 合并与改派（同日稍后）

- **PR #249（#244 选题五项正文编辑页）已合并**，合并提交 `18a98e8`。依据：CI 四项全绿；主控本地复核 `pnpm typecheck --force` 9/9、三项检查通过、core vitest 431 例、views vitest（content、locales、package-exports）365 例通过。`client.ts` 新增方法按 #176/#177/#195/#227/#228 先例随功能提交，不拆 `upstream:`。#244 保持打开，等用户按 PR 内手验 Todo 验完界面再关。
- **PR #247 CI 失败**：`store_integration_test.go:166` 断言的是 A2 之前的旧语义。按 spec 032 §3，配置未知的行展示 `unknown`，所以是测试过时而不是功能错误。会话 002 只改断言（`89106bb`），并做了二审：验收矩阵 7/7 通过，另报 F1–F4。
- **主控裁定 F1–F4**：F1 由服务端给空 reason 的行补固定原因码，页面不改；F2 加守卫测试，注册方法只许宿主装配代码调用；F3 以 spec 为准改设计记录；F4 配置未知且无证据时 health 报 `unknown`。本地子代理实现（`91f071b`、`9aea21b`、`f61c120`），主控补改设计记录第 98 行同一处矛盾（`77baf32`）。
- **PR #247 已合并**，合并提交 `efa964f`；#245 已关闭。依据：CI 四项全绿。
- **停用云端会话**：用户看到云端会话专用额度被扣约 $16（$250 赠送额度，11 月 5 日到期），表示不想动用。两个云端会话已空闲，不再派活；此后只派本地执行者。额外付费用量未开启，花费 $0。
- app 主检出已快进到 `efa964f`，用户的三处未提交改动保留。
- 待办：用户手验 #249 编辑页与诊断概览（组件行现在显示原因码，如 `config_unknown`、`storage_not_configured`）；本地开发库迁移仍由用户执行。
- **PR #252（#251 诊断原因码本地化说明）已合并**，合并提交 `24a917e`。依据：CI 四项全绿；子代理本地 `typecheck --force` 9/9、三项检查、locales parity 与 package-exports 通过；主控审阅页面改动只替换原因文字、未改样式。`packages/views/content/diagnostics` 下没有测试文件，页面由用户手验。
- 本地开发库迁移：用户本次批准由主控执行，但连接本地库的命令被自动模式权限分类器拒绝；主控启动的 PostgreSQL 随后台 shell 退出而停止。已把启动、查版本、备份、迁移四条命令交给用户在自己的终端执行。截至本条尚未迁移。

## 远程编译测试机与手验界面环境（老德国 VPS）

- 用户要求停掉老机的 Chatwoot、karakeep、libredesk、neko（共 11 容器，只 `docker stop`，数据卷保留，Chatwoot/libredesk 重启策略改为 no）。可用内存 4.0GB → 6.1GB。
- **远程编译测试机**：`~/loretide-ci`，只读部署密钥（GitHub deploy key，用户的 gh 添加），入口 `lt-verify.sh <分支|sha> [go|diag|db|node|all]`。每次起一次性 pgvector pg17 测试库（1GB 上限、不发布端口）和工具链容器（3GB 上限），测试库约定照搬 CI。对 app-main `24a917e` 验收：go build/vet 通过；诊断集成与执行开关 4 包通过；handler 3872 过 0 败 47 跳过；cmd-server 521 过；topic-planning 86 过；typecheck 9/9（`--force`，0 cached）与三项检查通过。
- 首次运行发现脚本假绿灯：typecheck 被 OOM 杀掉（137）却报 rc=0，原因是 `f || rc=1` 形式下 `set -e` 不生效。已改为逐步显式失败返回，typecheck 串行，重跑为真实通过。
- **手验界面环境**：`~/loretide-ui`，compose 项目 `loretide-ui`，从源码构建，只在 Tailscale 地址 `100.81.250.89:13000` 开放。开发模式、诊断模拟开启、真实执行器关闭，仅允许用户邮箱登录，使用固定开发验证码。启动时自动迁移到 539；健康检查与本机经 Tailscale 访问均为 200。空库，未迁入本机开发库数据。
- 本机开发库：主控启动时漏了端口参数，实例误跑在 5432（未连接），重启被权限拦截，用户已自行关闭。此后手验改用老机环境，本机库不再使用。

## 品牌运营扩展开工：规格 033 / 034（同日稍后）

- 用户决定先不手验，继续剩余开发。主控选定 BO-01（R-056 营销节点联动选题）与 BO-06（R-061 成本、线索、成交与 ROI 复盘）并行；BO-02 等 034 定稿后再写，BO-03 因需联网采集与执行器同为搁置，BO-04 需求未细化。
- **规格 034（PR #255，合并 `2ea0d85`）**：放在 feedback-learning；金额整数最小单位加币种，服务端确定性计算，D14-V12 固定样例写成测试。Q1–Q7 采用推荐值。否决给 feedback-learning 加 idempotency 依赖（用户约束：不改 modules 依赖表），导入幂等改在模块内自建只插不改的占位表。adapters 登记由实施 PR 带上。AI 解释与采纳为后续项，D14-V15 部分验收。
- **规格 033（PR #256，合并 `dc8c44c`）**：放在 topic-planning；候选由确定性规则加人工填写，AI 生成与联网节日日历为后续项。仓库无日历领域，撞期只在节点间比较，受影响对象为经节点采用的选题卡。唯一索引走独立并发索引迁移（子代理对照迁移规则检查代码确认不冲突）。
- 两份规格共同规则：迁移不预留号段，谁先合并谁先占号，后者合并前顺延。
- 规格作者发现 topic-planning `Create` 在写锁外校验账号，已登记为独立后续任务，不在本批。
- 已派出 033 PR 1（节点存储与接口）与 034 PR 1（记录存储与接口），均由本地子代理实现，不使用云端会话。
