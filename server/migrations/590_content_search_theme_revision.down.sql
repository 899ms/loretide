-- Dropping this table loses every search theme and every revision of it:
-- the questions, keywords, intent, origin and references people recorded.
-- 会丢失全部搜索主题.
DROP TABLE IF EXISTS content_search_theme_revision;
