# 手动验收清单：004 品牌工作区时区与切换隔离（LT-009）

按 constitution 原则 II，界面行为**不写 UI 单测、不用自动点击**，只能由用户在真实浏览器中确认。

**全部条目状态：待用户验证。** 未执行一律记「未执行」，不得因为「代码写了」或「测试绿了」标为通过。

前置：`make up` 起本地环境，用能创建工作区的账号登录。

## W — 创建与保持（US1）

| # | 步骤 | 预期 | 状态 |
|---|---|---|---|
| **004-W-1** | 创建工作区 A，时区选 `Asia/Shanghai` | 表单有「时区」选择，默认已预选 `Asia/Shanghai`；创建成功 | 待用户验证 |
| **004-W-2** | 再创建工作区 B，时区选 `America/Los_Angeles` | 创建成功，B 的时区为所选值 | 待用户验证 |
| **004-W-3** | **刷新页面**，分别打开 A、B 的设置 → 通用 | A 显示 `Asia/Shanghai`，B 显示 `America/Los_Angeles`，**各自不变** | 待用户验证 |
| **004-W-4** | 退出并重新登录，再看 A、B | 时区仍分别为上述值 | 待用户验证 |
| **004-W-5** | 在 A 的设置里把时区改为 `Europe/London` 并刷新 | 显示 `Europe/London`；**B 不受影响**；A 里已有内容的时间戳没有变化 | 待用户验证 |
| **004-W-6** | 看一个**本次之前**就存在的老工作区 | 时区显示 `Asia/Shanghai` 并带「默认」标记；**不显示空白** | 待用户验证 |
| **004-W-7** | 在 A 显式选择 `Asia/Shanghai` 后再看 | 仍显示 `Asia/Shanghai`，但**不再有「默认」标记**——「选了上海」与「没选过」要能分辨 | 待用户验证 |

## S — 切换隔离（US2，既有机制的复核）

| # | 步骤 | 预期 | 状态 |
|---|---|---|---|
| **004-S-1** | 在 A 建一条可见对象（任务 / Issue），切到 B | B 的列表**不出现** A 的对象 | 待用户验证 |
| **004-S-2** | 在 B 打开诊断页实时流，再切回 A | 流按新工作区重建，**不混入** B 的事件 | 待用户验证 |
| **004-S-3** | 切回 A | A 的数据完整显示，无需重新创建 | 待用户验证 |

> **004-S-1 ～ S-3 验的是上游既有机制**（查询键含 `wsId`、查询按 `workspace_id` 过滤），不是本特性新建的。发现任何缺 `wsId` 的查询键或未按 `workspace_id` 过滤的接口，**作为独立缺陷报告主任务**，不在本特性内修（tasks T013）。

## T — 中文术语一致（US4）

| # | 步骤 | 预期 | 状态 |
|---|---|---|---|
| **004-T-1** | 中文界面下看创建、切换器、设置三处 | 一律称「**工作区**」；**不出现**「品牌空间」作为界面用词 | 待用户验证 |

## 不在本清单内

- 非成员拒绝（US3）：已由 `TestWorkspaceTimezone_IsNotReadableByANonMember` 挂真实中间件自动覆盖，无需手验。
- 时区取值与默认的判定逻辑：已由 `timezone.test.ts` 17 条与 Go 8 条自动覆盖。
