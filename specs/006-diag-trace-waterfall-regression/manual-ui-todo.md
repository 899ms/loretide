# Manual UI Todo: 006 诊断 trace 瀑布视图与回归关联

**状态**：全部条目初始为「待用户验证」。按 `.specify/memory/constitution.md` 原则 II，UI 验收不写单测、不做自动点击；未执行的条目记「按策略未执行，等待用户验证」，**不记通过**。

**页面**：Web `/{workspaceSlug}/diagnostics`（例：`/loretide-dev-check/diagnostics`）。需以 workspace owner / admin 身份登录。桌面端未挂载诊断页（spec Assumptions）。

**前置**：本地实例已启动；已有若干已保存的模拟运行（`normal` / `slow` / `timeout` / `database` 各一次以上），其中至少一次是由另一次复现而来（带 `original_run_id`）。

## 子句覆盖

- **US1 瀑布层级与时间轴** → W-1 ～ W-8
- **US2 四态判定** → V-1 ～ V-6
- **US3 回归关联** → L-1 ～ L-5

## US1 · trace 瀑布（FR-001 ～ FR-004）

| # | 位置 | 操作 | 预期 | 对应 | 用户确认 |
|---|---|---|---|---|---|
| W-1 | 运行详情 → trace 标签页 | 打开一次含父子 span 的运行 | 子 span 相对父 span 缩进一级；层级与 `parent_span_id` 一致 | FR-001 | 待用户验证 |
| W-2 | 同上 | 观察横向位置 | 每行左边距对应其相对本次运行最早时刻的偏移；行与行之间的空白对应真实等待间隙 | FR-002 | 待用户验证 |
| W-3 | 同上 | 找两个时间重叠且互不为父子的 span | 二者在横轴上可见地重叠，而不是排成前后两行等长条 | US1 场景 2 | 待用户验证 |
| W-4 | 同上 | 打开 `clock_skew` 场景的运行 | 出现「时钟偏差」标注，并提示该处不可按时间解读；位置照实显示，未被夹取到父区间内 | FR-002a | 待用户验证 |
| W-5 | 同上 | 打开一次 span 的 `parent_span_id` 指向本次运行之外的运行 | 该 span 显示为顶层并标注「父 span 不在本次运行内」；未被丢弃、未挂到其他父节点下 | FR-001 | 待用户验证 |
| W-6 | 同上 | 打开一次只有单个 span 的运行 | 正常显示单行，不出现空图或异常宽度 | US1 场景 4 | 待用户验证 |
| W-7 | 同上 | 打开一次 span 数超过 200 的运行（如无，可跳过并注明） | 显示「另有 N 条」折叠提示；**失败步骤（含错误码的 span）及其各级父节点在折叠状态下仍可见** | FR-004 | 待用户验证 |
| W-8 | 同上 | 点开 W-7 的折叠提示 | 被折叠节点展开，层级仍然正确，无悬空片段 | FR-004 | 待用户验证 |

## US2 · 四态回归判定（FR-005 / FR-006）

| # | 位置 | 操作 | 预期 | 对应 | 用户确认 |
|---|---|---|---|---|---|
| V-1 | 运行列表 → 回归结果列 | 查看一条从未评估的运行（`regression` 为空） | 显示「未运行」，**不是空白单元格**，也不是通过 | FR-005 | 待用户验证 |
| V-2 | 同上 | 查看一条通过的运行 | 显示通过，且同一行/详情处可见 `expected_code` 与 `actual_code` 作为判定依据 | FR-006 | 待用户验证 |
| V-3 | 同上 | 查看一条失败的运行 | 显示未通过 | FR-005 | 待用户验证 |
| V-4 | 同上 | 阅读「通过」旁的说明文案 | 表达「模拟结果符合该故障预期」，**不**表达「该功能可用」 | US2 场景 4 | 待用户验证 |
| V-5 | 同上 | 若存在 `status` 与 `regression` 冲突的运行（如无，可跳过并注明） | 显示「无法判定」，并同时可见两个原始值 | Edge Cases | 待用户验证 |
| V-6 | 概览区 | 查看概览 | **不应**出现四态的聚合计数——本功能明确不做（FR-013）。若出现，即为越界 | FR-013 | 待用户验证 |

## US3 · 回归关联（FR-007 / FR-008）

| # | 位置 | 操作 | 预期 | 对应 | 用户确认 |
|---|---|---|---|---|---|
| L-1 | 运行列表 | 查看一条由复现而来的运行 | 原故障运行、场景、模块、代码版本四项均可见 | FR-007 | 待用户验证 |
| L-2 | 同上 | 点击原故障运行 | **在同一诊断页内**切换到该运行详情；浏览器地址栏未新增路由段 | FR-008 | 待用户验证 |
| L-3 | 同上 | 查看一条首次运行（`original_run_id` 为空） | 标注「本身即原故障」，**不是**「未记录」也不是空白 | FR-007 | 待用户验证 |
| L-4 | 同上 | 查看一条 `module` 或 `build` 为空的运行（如无，可跳过并注明） | 该项显示「未记录」，不是空白、不是推断值 | FR-007 | 待用户验证 |
| L-5 | 同上 | 若原故障运行已被保留期裁剪或不可读（如无，可跳过并注明） | 标注「原故障运行不可读」并给出原因类别；点击不进入错误页或空白页 | FR-008 | 待用户验证 |

## 按策略未执行

以上 **W-1 ～ W-8、V-1 ～ V-6、L-1 ～ L-5 共 19 条**全部按 constitution 原则 II 不做自动化，本次交付**未执行**，等待用户在本机浏览器验证。

标注「如无，可跳过并注明」的四条（W-7、V-5、L-4、L-5）依赖特定数据形态；若本机数据无法构造，**记「未执行 · 缺少对应数据」，不得记为通过**。
