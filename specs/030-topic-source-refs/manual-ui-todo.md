# 030 PR 2 Manual UI Acceptance

Status: **未执行 (not executed)**. Browser acceptance is performed by the user; this
file is a checklist, not evidence that any item has passed.

- [ ] In a new topic card, independently select one source for "why it fits
  this IP" and another for "evidence and gaps"; create the card and confirm
  each reference stays in its own column.
- [ ] In an existing topic card, add and remove a source from either column;
  confirm an explicit empty selection clears only that column and changing one
  column does not affect the other.
- [ ] Confirm "Relation to existing work" has no source selector.
- [ ] Archive a previously referenced source; confirm it remains visible in
  the card detail with its archived status, while it is absent from new
  candidates but remains removable from its selected-reference row.
- [ ] Confirm a previously stored source ID whose detail is unavailable still
  renders with its ID fallback and can be removed.
- [ ] While source-reference saving is pending, confirm all selector checkboxes
  are disabled and no post-submit selection is discarded.
- [ ] Confirm source choices are limited to the active workspace and a source
  from another workspace cannot be selected.
- [ ] Check long titles, URLs used as title fallbacks, and source IDs used as
  the final fallback for wrapping and non-empty display.
- [ ] Check a user without write permission sees the existing read-only/error
  behavior and cannot successfully save source-reference changes.
- [ ] Repeat the source-reference flow in English, Simplified Chinese,
  Japanese, and Korean.
