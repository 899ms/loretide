# 手动 UI 清单（033 营销节点页面）

**状态：全部未执行。** 这是一份给用户在浏览器里逐条核对的清单，不是任何一条已经通过的证据。本卡不写 UI 单测，也不用 computer use 做验收（宪法 II）。PR 3 交付时按实际实现更新入口与组件说明，条目状态保持「未执行」。

**入口**：`/{workspaceSlug}/marketing-nodes`，侧栏内容区「选题」之后一项（D1 已批准，以 PR 3 实际为准）。

**准备数据**：品牌 A 有两个账号（其中一个的账号配置里「受众」没填）、三条收件箱素材（其中一条之后要归档）；另建一个品牌 B。品牌时区保持默认 Asia/Shanghai。

**复用的既有组件**：设置页布局（`SettingsContent` / `SettingsTab` / `SettingsSection` / `SettingsCard` / `SettingsRow`）、`Button`、`Input`、`Textarea`、既有下拉与复选组件。PR 3 实施后在这里补上实际用到的清单。

## 本清单最要紧的四条

- **U-05**：提前量「未设置」与「0」一眼能分开。
- **U-09**：采用只多一张草稿选题卡，别的什么都没发生。
- **U-12**：改期后系统没有替你改任何一张卡。
- **U-14**：品牌 B 里看不到品牌 A 的同名节点。

## 手验项

| 编号 | 手动项 | 期望 | 状态 |
|---|---|---|---|
| 033-U-01 | 入口与视觉 | 侧栏有「营销节点」，图标不是 Issues 的那个；页面排布、字号、语义色与选题页、素材页一致，没有本页独有的控件或配色 | 未执行 |
| 033-U-02 | 新建节点 | 新建「双十一」：营销节点、2026-11-11～2026-11-11、提前 14 天、两个适用账号各写一个角色、运营目标、引用两条素材、日期确定。保存后列表出现，详情逐项与填写一致；时区一栏**默认已填品牌时区** | 未执行 |
| 033-U-03 | 阶段与「今天」 | 列表与详情写明「按 Asia/Shanghai 的 YYYY-MM-DD 计算」，阶段与这一天相符（准备期未到 / 准备中 / 进行中 / 已结束） | 未执行 |
| 033-U-04 | 另一个时区 | 再建一个同日期、时区 America/Los_Angeles 的节点；在北京时间凌晨 0:00–8:00 之间看，两个节点的「今天」**相差一天**，阶段可能不同 | 未执行 |
| 033-U-05 | **提前量：未设置 ≠ 0** | 一个节点提前量留空、另一个填 0：前者写「未设置提前量」、「够不够」一项不下结论；后者写「不需要准备」。两者**一眼能分开** | 未执行 |
| 033-U-06 | 跨年节点 | 建 2026-12-31～2027-01-02、提前 10 天的节点，准备期显示从 2026-12-21 起 | 未执行 |
| 033-U-07 | 修改与历史 | 把「双十一」提前量改成 21：历史里两个版本，第 1 版仍是 14，第 2 版标「改期」；只改运营目标的一次标「修改」 | 未执行 |
| 033-U-08 | 表单当场拦 | 结束日早于开始日、提前量填 −1 或 1.5、时区清空：保存不可点，并点名是哪一项 | 未执行 |
| 033-U-09 | **采用只建草稿卡** | 整理候选 → 给一条写切入角度 → 采用并新建。页面按钮旁写着「只建一张草稿选题卡，不会开始创作、提交审核或发布」。去选题页：多了**一张草稿卡**，账号对、「为什么是现在」有节点名与日期、「为什么适合这个 IP」是刚写的角度、立论素材是节点的素材；这张卡下**没有**简报、开始记录、作品、审核、交付 | 未执行 |
| 033-U-10 | 候选的内容 | 每条候选显示：账号的受众 / 内容支柱 / 内容目标（没填的写「未填写」，**不是空白**）、节点目标与账号角色、准备期、撞期、资料缺口、重复风险、来源与日期依据。候选区有一句恒定说明「候选由……整理而来，切入角度需要你来写；系统不会生成选题」。**没有任何一句像是系统写的推荐语** | 未执行 |
| 033-U-11 | 缺口与重复 | 归档节点引用的一条素材 → 候选标「引用的素材已归档」；在某账号下建一张正文里写了「双十一」的卡 → 该账号候选出现重复风险并能点到那张卡；再建一个同期、同账号的节点 → 两边候选都标撞期并互相点名 | 未执行 |
| 033-U-12 | **改期不改卡** | 把已采用过的「双十一」改到 11-18：详情出现影响清单，列出那张卡、采用时第几版、现在第几版、日期从哪天到哪天。打开那张卡：**内容与改期前完全一样** | 未执行 |
| 033-U-13 | 影响决定与重复操作 | 在影响清单里标「保持不变」→ 清单清空；再改期一次 → 那一条重新出现。连点两次「整理候选」、连点两次「采用」：候选数不变，选题页也只多一张卡 | 未执行 |
| 033-U-14 | **品牌隔离** | 在品牌 B 也建「双十一」：A 的列表只有 A 的，B 的只有 B 的；在 B 导入 A 已有的同一行，不报重复 | 未执行 |
| 033-U-15 | 导入与确认 | 贴 3 行 CSV（1 行与已有节点同名同开始日）：结果逐行显示「已建 / 重复 / 哪列不合法」；新建的两个是「待确认」，不能整理候选；确认后可以；一个写着「日期未确定」的节点确认后**仍显示日期未确定** | 未执行 |
| 033-U-16 | D14-V08 前半段 | 走通：建节点 → 整理候选 → 写角度 → 采用 → 在选题页打开那张卡 → 按既有流程建作品、登记发布、录入手工反馈。**「→ 诊断」一段本卡不含（依赖 BO-02），记「未执行（依赖 BO-02）」；本卡没有模拟结果、没有联网结果，「分别标识」记「本卡无此对象」** | 未执行 |
| 033-U-17 | 取消 | 取消一个节点：状态「已取消」，编辑、确认、整理候选的按钮不可用；影响清单列出经它采用的卡并标「节点已取消」 | 未执行 |
| 033-U-18 | 长文与溢出 | 很长的节点名、运营目标、角色、切入角度、日期依据：换行正常，不撑破布局 | 未执行 |
| 033-U-19 | 加载与失败态 | 断网刷新：各区块显示自己的失败态，不整页空白；恢复后正常 | 未执行 |
| 033-U-20 | 无写权限 | 用只读成员打开：能看，保存 / 采用 / 取消得到既有的无权限表现 | 未执行 |
| 033-U-21 | 四语言 | 英文、简体中文、日文、韩文各走一遍 U-02、U-09、U-12：没有缺失文案、没有 key 名露出 | 未执行 |
