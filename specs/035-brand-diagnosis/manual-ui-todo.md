# 手动 UI 清单（035 品牌/账号经营诊断）

页面入口：`/{workspaceSlug}/operating-diagnosis`，五个区块：「生成诊断」「报告」「作品标注」「建议与决定」「待办与提议」。

存储、计算与接口由 PR 1～3 交付；本清单只覆盖 PR 4 的页面与 D14-V08 的浏览器闭环。全部条目**状态：未执行**——由用户或主控在浏览器逐条验收；执行会话不做 computer use，不写 UI 单测（宪法 II）。

复用的既有组件：`SettingsSection` / `SettingsCard` / `SettingsRow`（`@multica/views/settings/layout`）、`Button` / `Input` / `Textarea` / `Select`（`@multica/ui`）。不新建控件，不设颜色。

## 本清单最要紧的五条

**U-10、U-12、U-14、U-24、U-40。** 它们对应「未知不是 0」、不同平台不混排、不下因果结论、拒绝什么都不改、以及 D14-V08 的浏览器闭环。前四处任何一处在界面上走样，诊断就会误导人，而且不会有任何报错。

## 手验项

| 编号 | 手动项 | 期望 | 状态 |
|---|---|---|---|
| 035-U-01 | 页面位置与视觉 | 侧栏或路由能到 `/operating-diagnosis`，导航名「经营诊断」，图标与「开发诊断」不同（是 `Stethoscope` 或 PR 4 正文写明的替代图标）；五个区块的排布、字号、语义色与账号页、选题页一致；320 / 768 / 1024 / 1440 px 下无横向溢出 | 未执行 |
| 035-U-02 | 与开发诊断分开 | 开发诊断页（`/diagnostics`）里看不到任何经营诊断报告；经营诊断页里没有开发诊断的日志、追踪或故障模拟入口 | 未执行 |
| 035-U-03 | 范围选择 | 「账号」只能选一个；「品牌汇总」逐个勾账号，一个都不勾时「生成」不可点，说明写「请至少选一个账号」 | 未执行 |
| 035-U-04 | 窗口与时区 | 窗口日期旁显示品牌时区；结束早于开始时「生成」不可点；选了「表现变化」但没填对比窗口，能生成，该维度显示「不可计算：缺少对比窗口」 | 未执行 |
| 035-U-05 | 维度可配置、没有行业角色 | 六个维度可任意勾选（全不勾也能生成「只看范围」的报告）；页面上没有「行业」「岗位」「运营角色」之类的选项 | 未执行 |
| 035-U-06 | 分析范围 | 报告第一块写明账号、两个窗口、账号配置版本、各类输入的条数；「数据来源：仅人工录入」标识可见 | 未执行 |
| 035-U-07 | 一致性检查清单 | 在「作品标注」里给一条作品的「定位」标「不符合」→ 重新生成后一致性维度该项「不符合 1」；账号配置里未确认的项标「账号配置未确认」 | 未执行 |
| 035-U-08 | 支柱覆盖 | 参数写三个支柱，给作品标支柱 → 报告显示各支柱条数与「未标注」条数，旁边写「一条作品可属于多个支柱，各支柱条数不可相加」；支柱清单留空 → 「不可计算：未指定内容支柱」 | 未执行 |
| 035-U-09 | 节奏：未设定与 0 | 经营规则里小红书不设节奏、抖音设 0 → 小红书显示「目标：未设定」且没有「达成 / 未达成」；抖音显示「目标 0」并有对比；窗口首尾不完整的周标「不完整周」 | 未执行 |
| 035-U-10 | **未知不是 0** | 一条发布记录的收藏录为空（未知）、另一条录 0 → 表现维度「有值 1、未知 1、合计 0」；页面上没有把未知显示成 0、空白、∞ 或 NaN | 未执行 |
| 035-U-11 | 统计口径不一致 | 同一平台同一指标的两条采样，统计窗口分别写「发布后 7 天」「发布后 14 天」→ 该组标「统计口径不一致」并列出两种写法 | 未执行 |
| 035-U-12 | **不同平台不混排** | 小红书与抖音各有数据 → 表现变化按平台分两块；没有跨平台的合计行、排名列、「最好 / 最差」标记；品牌汇总里账号按名字排列，不按任何数字排序 | 未执行 |
| 035-U-13 | 两窗口之差 | 对比窗口均值 20、本窗口 15 → 显示「两窗口均值之差 −5.00」，用次要文字色，不用红色 | 未执行 |
| 035-U-14 | **不下因果结论** | 报告里任何系统生成的文字都没有「增长」「提升」「下降」「导致」「带来」「因为」；表现维度的局限说明里有「差值只描述两个窗口的数字，不说明原因」 | 未执行 |
| 035-U-15 | 受众反馈 | 按来源（评论 / 私信 / 线索）与平台计数；按标签计数，旁边写「一条摘录可有多个标签，不可相加」；页面上看不到摘录原文 | 未执行 |
| 035-U-16 | 执行流程 | 待审核、要求修改、窗口内被拒、交付暂停、计划时间已过未交接、发布失败 / 状态未知 / 已下架、已过观察时点没有指标，各自列出并能点到原记录；没有「慢」「超时」之类的判断 | 未执行 |
| 035-U-17 | 补录待办 | 缺数据时报告下方列出补录待办，每条写缺什么、在哪条记录、去哪里补，链接能跳到账号设置 / 经营规则 / 反馈录入页；点「加入待办」出现在「待办」区；再点一次提示已加入，不重复 | 未执行 |
| 035-U-18 | 账号未知 | 品牌汇总里有一条历史导入作品的发布记录 → 出现「账号未知」一节，并有补录待办；报告的局限里写明「有 N 条历史导入发布记录因无法关联账号而归入账号未知」；账号报告里同样写明未计入的条数 | 未执行 |
| 035-U-19 | 旧版本不变 | 生成版本 1 后补录一条指标 → 打开版本 1 数字不变，顶部提示「有 1 条输入在此版本之后新增或修改」；点「用最新数据生成新版本」得到版本 2，版本 1 仍在，版本 1 上写的判断没有搬到版本 2 | 未执行 |
| 035-U-20 | AI 判断占位 | 报告里「AI 判断」一栏显示「待运行：暂不可用（执行器禁用）。本页没有任何 AI 生成的内容。」；没有「生成 AI 建议」按钮 | 未执行 |
| 035-U-21 | 有依据的判断 | 写判断选「有数据依据」，从报告里点选一项引用 → 保存后判断旁显示引用，点引用能定位到报告那一项；不选引用时保存不可点 | 未执行 |
| 035-U-22 | 定性判断 | 选「定性判断」→ 不需要引用，保存后标明「定性判断：没有数据支持」 | 未执行 |
| 035-U-23 | 替代解释与局限 | 替代解释可以指向一条判断并显示在它下面；局限单独列出；三种都标「人工撰写」 | 未执行 |
| 035-U-24 | **拒绝什么都不改** | 拒绝一条「账号配置修改」建议 → 账号设置页的配置版本不变、选题列表不变、待办不变；建议旁显示「已拒绝」与拒绝人、时间 | 未执行 |
| 035-U-25 | 采纳为选题卡（新建） | 采纳一条选题卡建议选「新建」→ 选题列表多一张草稿卡，IP 适配理由里是建议正文；卡没有被开始，也没有任何创作被启动；建议旁显示「已采纳 → 选题卡 X」 | 未执行 |
| 035-U-26 | 采纳为选题卡（关联已有） | 选「关联已有」→ 只能选同账号（或未挂账号）的卡；选题卡本身不变 | 未执行 |
| 035-U-27 | 采纳失败与重试 | （需开发者制造建卡失败）建议显示「已采纳，动作失败」与原因；可以重试或改为关联已有卡 | 未执行 |
| 035-U-27a | 结果未记录后重试不多建卡 | （需开发者制造「卡已建、结果未记」）建议显示「已采纳，结果未记录」；点重试后选题列表里这条建议只对应一张卡，建议旁显示「已采纳 → 选题卡 X」；再点一次重试不可用 | 未执行 |
| 035-U-28 | 采纳为待办 | 采纳一条待办建议 → 「待办」区多一条，来源写明「由建议采纳」；可以改为完成或放弃 | 未执行 |
| 035-U-29 | 采纳为配置提议 | 采纳一条配置修改建议 → 「提议」区多一份，**账号设置页的配置没变**；提议逐项显示「当前值 → 提议值」 | 未执行 |
| 035-U-30 | 确认提议 | 点「确认修改」→ 账号设置页出现新版本，被修改的项为新值并标已确认，其余项不变；提议显示「已确认，账号配置第 N 版」 | 未执行 |
| 035-U-31 | 提议期间配置被改 | 采纳提议后先去账号设置页改一下配置，再回来确认 → 提示「账号配置已被修改，请刷新后再核对」，不写 | 未执行 |
| 035-U-32 | 放弃提议 | 点「放弃」→ 提议显示「已放弃」，账号配置不变 | 未执行 |
| 035-U-33 | 一个修订只能决定一次 | 已采纳或拒绝的建议，采纳 / 拒绝按钮不可点；修改建议后（新修订）才能对新修订再决定，旧决定仍显示在历史里 | 未执行 |
| 035-U-34 | 没有经营记忆入口 | 页面上没有「存入经营记忆」「写入经营结论」一类的入口；采纳目标只有选题卡、待办、配置提议三种 | 未执行 |
| 035-U-35 | 跨品牌 | 切到另一个品牌，用上一个品牌的报告 URL 直接打开 → 显示「不存在或无权访问」，与真的不存在一样；生成诊断的账号选择里看不到别的品牌的账号 | 未执行 |
| 035-U-36 | 中性颜色 | 「不可计算」「未设定」「未知」「账号未知」、负的差值都用次要文字色；页面上没有红黄绿表示好坏 | 未执行 |
| 035-U-37 | ROI 引用（Q6=A 时） | 生成时选一个 034 报告版本 → 报告里出现「经营结果（引用）」一块，原样显示那一版的数字与版本号，旁边写「原样引用，未参与本诊断的任何计算」 | 未执行 |
| 035-U-38 | 四语言 | 依次切英文、简体中文、日文、韩文，五个区块与报告都有对应文案，没有原始 key；各语言的导航名都与「开发诊断」不同 | 未执行 |
| 035-U-39 | 空品牌 | 新品牌一条数据都没有 → 生成的报告每个维度显示「不可计算：没有数据」（执行流程显示空清单），补录待办列出配置类缺口，页面不报错 | 未执行 |
| 035-U-40 | **D14-V08 浏览器闭环** | 在营销节点页建节点 → 整理候选并采用为选题卡 → 从选题卡建作品 → 提交审核并批准 → 交付并登记发布记录 → 在反馈页录一条指标与一条摘录 → 在经营诊断页生成报告，确认这条发布记录出现在范围与表现变化里 → 写一条选题卡建议并采纳 → 选题列表出现新草稿卡。全程报告标「仅人工录入」，没有模拟或联网结果 | 未执行 |

## 本卡不能手验的部分

D14-V08 的「真实联网分别验收」：本版没有联网采集，也没有模拟结果，记「不适用（本版无联网；等 AI 判断层或 BO-03 监测落地后另验）」，不能用本清单代替。
