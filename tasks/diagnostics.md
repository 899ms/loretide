# Loretide · 初期完整诊断任务

日期：2026-09-13。本文件为 DIAG 任务状态来源。已有诊断实现；下方未勾选项表示尚未逐项完成主任务验收，不能理解为没有代码。依据 [完整诊断需求](../docs/13-完整开发诊断与操作日志需求.md)。

每项预计1～5个手写文件，超过时拆子任务。实际实现位于 app/server/internal/content/diagnostics、handler 接入层及 core/views 的 content/diagnostics。Windows 本机独立数据库回归已执行，见[本轮验收记录](../records/2026-09-13-本机独立数据库回归与并发审查.md)。历史子任务工作树的状态未自动回写此目录，本次按实际证据校准，DG-01 尚未整体通过。

## 本轮核验状态

- 已完成：本机独立测试库、诊断 PostgreSQL 集成、完整 handler 回归（外部依赖跳过单列）、workspace 删除与诊断写入竞态核验。
- DIAG-08 / DIAG-12：已有实现，实时流及下载前端定向测试通过；完整浏览器验收矩阵仍需逐项核对，暂不标记总体验收完成。
- 待完成：诊断公共能力出口逐项证据核对、真实执行器安全验收、应用代码基线发布。没有将 Redis 或外部服务跳过项记为通过。

## DIAG-01 · 诊断合同与模块接口
- [ ] **TODO**；依赖：LT-002、ARCH-01；规模S；验收D13-V02/04。
- 交付 operation/trace/span/attempt、审计/技术日志、错误码、组件状态的合同与可注入接口；明确数据所有权。
- 验证序列化、错误分类、未知字段与调用者兼容；没有全局业务依赖或私有SQL入口。
- 预计位置：content诊断类型、Go接口、合同测试。

## DIAG-02 · 脱敏与日志字段策略
- [ ] **状态：部分交付，待验收**（PR [899ms/loretide#30](https://github.com/899ms/loretide/pull/30)，2026-09-14）；依赖：DIAG-01；规模M；验收D13-V08。本次闭合：请求头三档准入（逐名白名单、敏感头一律不记）、路由身份取注册模式而非原始路径、Sanitize 二次校验；剩余：模型输出样例负例测试（派发中）。
- 交付凭据、请求头、路径/URL与私有正文的字段准入和脱敏规则，供全部sink及导出复用。
- 验证嵌套字段、异常文本、模型输出样例，不能只测试顶层API key；不记录真实凭据。
- 预计位置：redact适配、策略、负例测试。

## DIAG-03 · 技术日志采集与有界存储
- [ ] **TODO**；依赖：DIAG-02、LT-003；规模M；验收D13-V05/11。
- 交付结构化slog适配、有界缓冲、滚动存储/索引、级别/保留配置与丢弃可见状态。
- 验证写失败、磁盘满模拟、截断及持续写入；sink失败不拖垮普通请求。
- 预计位置：logger适配、存储、配置和测试；存储实现复杂时继续分卡。

## DIAG-04 · 操作审计存储与一致性
- [ ] **状态：部分交付，待用户验收**（PR [899ms/loretide#30](https://github.com/899ms/loretide/pull/30)、[#45](https://github.com/899ms/loretide/pull/45) 已合入 app-main `55189a835`，2026-09-15）；依赖：DIAG-02、LT-005；规模M；验收D13-V02/11。已闭合：事务内登记 + 提交后派发的 Outbox 接口（#30）；持久 outbox（#45）：`content_dispatch_outbox` 表（迁移 474～476，无外键、索引 `CONCURRENTLY`）与业务写入同事务登记，进程内租约排水器周期认领，重启不丢、租约过期可重领、并发不重复派发、幂等键去重，真实 PostgreSQL 53 用例通过；`Store.Audit`/`CommitRun` 语义不变。不声称 exactly-once（派发后标记前崩溃的窗口由接收方幂等吸收）。后续：排水器接入服务级优雅停机。
- 交付追加审计、事务/outbox接口与授权查询，技术日志清理不能清除审批证据。
- 验证成功/失败/回滚/重复事件，关键审计失败不得伪报业务成功；遵守无FK/并发索引迁移约束。
- 预计位置：审计存取、迁移、服务测试；生成代码不手改。

## DIAG-05 · 贯通异步追踪
- [ ] **状态：部分交付，待验收**（PR #30，2026-09-14）；依赖：DIAG-01、LT-006；规模M；验收D13-V03。本次闭合：全局 HTTP trace 传播中间件（边界自铸 trace、入站 traceparent 只作关联属性、daemon 认证后才采信）、daemon 出站注入 traceparent、X-Diagnostic-Trace 全路由回传；「非诊断路由只传播不记录」有 Go 测试。
- 交付HTTP、队列消息、WebSocket/模拟daemon与结果回写的trace传播，重试attempt及去重序号。
- 验证父子链、取消、重复/迟到、格式非法或伪造ID；ID不作为授权。
- 预计位置：middleware/协议适配及测试，必要时按传输拆子任务。

## DIAG-06 · 前后端结构化错误接入
- [ ] **TODO**；依赖：DIAG-03、DIAG-05、LT-007；规模M；验收D13-V04。
- 交付统一错误响应、浏览器错误边界/请求错误关联、步骤及下一动作；诊断详情按权限展示。
- 验证权限/文件/网络/schema等错误，以及未捕获UI异常的可定位记录。
- 预计位置：handler错误适配、core API、Web边界及测试。

## DIAG-07 · 健康、心跳与运行指标
- [ ] **TODO**；依赖：DIAG-03、DIAG-05、LT-007；规模M；验收D13-V01/11。
- 交付组件健康汇总、心跳过期、版本、队列与耗时/错误/丢弃统计；尚未连接执行器明确未验证。
- 验证组件断开、慢响应、样本不足、时钟偏差及有限指标标签，不默认触发真实模型探测。
- 预计位置：健康聚合服务、指标适配、配置与测试。

## DIAG-08 · 授权诊断查询与实时流
- [ ] **状态：部分交付，已合并，待用户验收**（PR [899ms/loretide#21](https://github.com/899ms/loretide/pull/21) 经 [#20](https://github.com/899ms/loretide/pull/20) 合入 app-main，2026-09-14）；依赖：DIAG-03、DIAG-04、DIAG-05；规模M；验收D13-V05。本次闭合：计划轮换对用户不可见（`rotate` 收尾页）、重连携带过滤条件与游标、退避重连、授权失败停止重连；D13-V05 浏览器矩阵在 `specs/002-.../manual-ui-todo.md`，待用户本机验证。
- 交付审计/技术日志/trace的授权过滤、分页、流式游标、去重与缺口状态；先用身份/空间权限夹具验证，账号业务完成后同步接入。
- 验证跨品牌/账号拒绝，暂停续读、游标过期及乱序；查询不能直接修改业务状态。
- 预计位置：诊断读服务、handler/流协议、定向测试。

## DIAG-09 · 完整浏览器诊断面板
- [ ] **状态：部分交付，待用户验收**（PR [899ms/loretide#37](https://github.com/899ms/loretide/pull/37) 已合入 app-main `30ecd87d0`，2026-09-14）；依赖：DIAG-06、DIAG-07、DIAG-08、ARCH-02；规模M，超出则按视图分卡；验收D13-V01～05。本次闭合：trace 标签页改为层级瀑布（父子缩进、相对运行起点的时间轴、超过 200 事件折叠且保住失败步骤、可展开）；逻辑下沉 `packages/core/content/diagnostics/{trace-waterfall,regression}.ts` 有纯函数测试，UI 层无自动测试；20 条手动 UI 项在 app `specs/006-diag-trace-waterfall-regression/manual-ui-todo.md`，待用户本机验证。
- 交付概览、操作时间线、日志筛选/实时流、trace瀑布及运行步骤入口；支持复制追踪编号和关联跳转。
- 验证浏览器从操作到失败步骤定位、分页/暂停、断线提示和开发权限；不依赖Electron。
- 预计位置：core诊断客户端、views诊断视图、Web路由及浏览器测试。

## DIAG-10 · 输入快照与复现清单
- [ ] **TODO**；依赖：DIAG-01、DIAG-04；规模M；验收D13-V06/10。
- 交付版本/授权/文件哈希/模型参数的通用快照记录和复现缺口清单，用模拟输入先验证。
- 验证配置不可回写历史、文件缺失/变化、授权撤销；不复制媒体或在日志中保存完整私有资料。
- 预计位置：快照合同/服务、查询与测试；业务对象实际接入随对应LT/EP交付。

## DIAG-11 · 模拟执行器与固定故障场景
- [ ] **TODO**；依赖：DIAG-05、DIAG-10；规模M，场景多时拆子卡；验收D13-V07/10。**对表（2026-09-14，主任务本机核对 app-main `c9cc63eca`）**：docs/13 §7 列举的 15 项故障（正常、慢响应、超时、取消、断线/重连、重复、迟到、文件缺失、文件改变、授权拒绝、数据库写失败、模型认证、模型额度、输出格式、搜索失败）全部存在于 `server/internal/content/diagnostics/simulator.go` 的 `Scenarios`（16 个，另有 `clock_skew`）；D13-V07「全部列举故障」子句的对表闭合，可独立复现与浏览器侧仍待用户验证。存储约束核对：迁移 468～473 无 `REFERENCES`/`CASCADE`，5 条索引全部 `CREATE INDEX CONCURRENTLY`。
- 交付可控时间/种子及需求§7全部故障场景、隔离复现入口；不默认调用真实模型。
- 验证场景可重复、取消/迟到/数据库失败等可定位，非测试环境注入拒绝；不能执行人工批准或发布动作。
- 预计位置：模拟执行器、fixtures、故障注入门禁及测试。

## DIAG-12 · 诊断包与保留策略
- [ ] **状态：部分交付，已合并，待用户验收**（PR [899ms/loretide#21](https://github.com/899ms/loretide/pull/21) 经 [#20](https://github.com/899ms/loretide/pull/20) 合入 app-main，2026-09-14）；依赖：DIAG-02、DIAG-08、DIAG-10；规模M；验收D13-V08/11。本次闭合：下载保存服务端原始字节与服务端文件名（不再经前端 schema 重序列化）、非授权 run/account 拒绝且不生成文件、缺口提示保留、200 条上限标示；D13-V08/V11 浏览器矩阵待用户本机验证。
- 交付脱敏导出清单/预览、权限检查、审计与技术日志独立保留/清理、容量状态。
- 验证跨账号导出拒绝、密钥和正文负例、日志清理不删批准记录；不自动上传诊断包。
- 预计位置：导出/生命周期服务、预览接口及测试。

## DIAG-13 · 回归关联与诊断端到端验收
- [ ] **状态：D13-V12 部分交付，待用户验收**（公共接入合同规格 PR [899ms/loretide#33](https://github.com/899ms/loretide/pull/33)、实施 PR [899ms/loretide#35](https://github.com/899ms/loretide/pull/35) 已合入 app-main `b11803fd9`，2026-09-14）；依赖：DIAG-09、DIAG-11、DIAG-12；规模M；验收D13-V09/12及全部D13基础场景。本次闭合 D13-V12 第一、三子句：`docs/development/diagnostics-onboarding-contract.md`（消费方 E1/E2/E3、提供方 P1–P3、模拟/真实证据分栏）、`pnpm check:diagnostics-contract` 本地与 CI 同源、豁免登记 `scripts/diagnostics-contract.json` 须带到期日期、PR 模板两条检查项。第二子句（真实 Codex 实测）在执行器禁用期间不可能通过，对照表 V12 标「部分」。D13-V09 回归四态判定（通过/未通过/未运行/无法判定，前端从 `regression`+`status` 推导，`"not_run"` 与空串均为未运行）与原运行/场景/模块/提交四项定位随 #37 合入，待用户按 006 手动清单验证。
- 交付面板中的故障复现与回归结果入口、故障/场景/模块/提交关联、CI模拟检查和浏览器验证报告。
- 验证修复前故障用例失败、修复后通过，未执行不显示通过；浏览器可从操作记录定位技术原因并导出脱敏包。
- 预计位置：回归结果适配、诊断视图扩展、CI入口、浏览器测试和记录。

## DG-01 · 初期公共诊断能力出口

- [ ] DIAG-01～13 完成，D13-V01～11用完整模拟场景验证，D13-V12的公共接入合同通过。LT-009 开始前必须完成此检查点。
- [ ] 实际业务/文件/Codex/远程组件尚未实现的状态明确展示，不把模拟健康当真实验证。
- [ ] 后续每个LT/EP交付同时完成D13-V12对应模块的真实接入和针对性回归，不能以DG-01通过豁免。

依赖不回指LT-009：使用现有身份、独立数据库和模拟协议先实现公共诊断。品牌/账号/文件/作品功能实际出现时接入相同接口并验收。首轮不是最小版，任何列举能力不得改标为后续可选增强。
