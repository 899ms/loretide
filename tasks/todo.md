# Loretide · 开发任务清单

日期：2026-09-13。主功能任务状态来源；新增架构前置任务状态见 [architecture.md](architecture.md)，完整诊断任务状态见 [diagnostics.md](diagnostics.md)。计划与目录说明见 [plan.md](plan.md)，业务决定见 [开发基线](../docs/11-首版开发基线与实施清单.md)。

**当前状态：LT-001～002、CP-01 已完成；LT-003/005/006/007 进行中，其余任务待实施。** 24 个近期功能任务＋2 个架构前置任务＋13 个完整诊断任务＋6 个后续阶段项；主清单9个检查点，诊断另有DG-01。卡片中的路径相对 app 工程，未完成卡片的路径仍为“预计涉及”；文档产物放相应工程 docs/development，最终位置在实施时登记。

## W-01：独立开发实例

### LT-001 · 建立应用工程来源与 Git 边界
- [x] **状态：DONE**；依赖：无；规模：S；追踪：T-01、D10-V01。证据：[初始化记录](../records/2026-09-13-应用工程初始化记录.md)。仅源码/Git 初始化完成，浏览器运行验收仍待后续任务。
- **范围/交付**：核对独立 app 位置和远程用途，从固定 Multica 提交建立实施 checkout，保留上游历史/许可并记录来源。现有研究库和文档库保持独立；不擅自覆盖已有 multica 远程。
- **验收**：起始提交可核对；文档仓库不出现嵌套 gitlink；应用未来改动不会写入 research。
- **验证**：Git HEAD、status、remote 和根 ignore 检查；记录克隆深度及后续取历史方式。
- **预计涉及**：应用 Git 配置、docs/development/source-baseline.md（新增），文档任务记录。

### LT-002 · 核验 Windows 开发工具
- [x] **状态：DONE**；依赖：LT-001；规模：S；追踪：T-01。证据：[工具核验记录](../records/2026-09-13-Windows开发工具核验记录.md)；已核验缺口，尚未安装补齐。
- **范围/交付**：核对 Go、Node、pnpm、数据库客户端及可用数据库运行方式，形成当前版本/缺口表和安装配置步骤。
- **验收**：区分已存在、需安装、实际不可用；不把 bundled runtime 或上游脚本存在当成应用已启动。
- **验证**：版本命令和 package/go 配置对照，不输出环境秘密；无无关全局版本覆盖。
- **预计涉及**：package.json（必要时）、docs/development/windows.md（新增）。

### CP-01 · 来源和工具
- [x] LT-001～002 均完成；独立 checkout 与版本记录可复核；未改动研究库，当前启动阻塞明确。

### LT-003 · 定义独立实例配置
- [ ] **状态：IN PROGRESS**；依赖：LT-002；规模：M；追踪：R-001、NFR-03、T-01。
- **范围/交付**：配置独立开发数据库名、附件区、API/Web 端口与实例标识，保存无秘密的样例配置；本机实际配置忽略。
- **验收**：配置缺失报错可读；端口冲突不杀其他进程；与日常实例数据目录不同。
- **验证**：配置解析/缺字段负例，检查端口及路径归属；记录实际选用原生数据库或容器的理由。
- **预计涉及**：.env.example、配置读取入口、docs/development/windows.md。

### LT-004 · 提前核验执行器隔离风险
- [ ] **状态：TODO**；依赖：LT-002；规模：M；追踪：R-030、NFR-03、D10-V02/03。
- **范围/交付**：核对 Codex 入口、登录可用性的非秘密状态、auth 链接/复制路径、宽权限与 OpenClaw 注册路径；形成修复清单并保证未验证真实执行不可启用。
- **验收**：不复制令牌或实际调用模型；不支持隔离时明确拒绝；OpenClaw 移除和安全适配落实到 EP-08 子任务，不被遗漏。
- **验证**：源码位置、客户端版本、能力状态与禁用门禁的针对性检查；模拟执行可单独标识。
- **预计涉及**：server/pkg/agent、server/internal/daemon 中实际入口（先定位）、docs/development/runtime-gaps.md。

### LT-005 · 跑通独立开发数据库
- [ ] **状态：IN PROGRESS**；依赖：LT-003；规模：M；追踪：NFR-04、D10-V06、T-01。
- **范围/交付**：按已选方式启动独立数据库，执行原基准迁移并记录连接及启动步骤，尚不创建全部内容表。
- **验收**：重跑迁移无重复对象；停启不丢数据；日志不暴露连接密码。
- **验证**：迁移执行和空数据库再次迁移验证，检查实例名与数据库目标；实际失败留证据。
- **预计涉及**：开发数据库配置、迁移入口（如有兼容修复）、docs/development/windows.md。

### CP-02 · 配置、风险、数据库
- [ ] LT-003～005 完成；数据库独立且可恢复；未验证真实执行仍禁用；执行器风险有具体后续项。

### LT-006 · 启动 Go API 并验证身份入口
- [ ] **状态：IN PROGRESS**；依赖：LT-005；规模：M；追踪：R-001、NFR-03、T-01。
- **范围/交付**：复用上游身份/API，在开发实例跑通健康检查与本人访问，不另造身份系统。
- **验收**：健康正常；未认证请求无法取业务数据；跨来源写请求遵守已有认证/CSRF约束。
- **验证**：受影响 Go 包检查和健康/未认证/有效会话请求；端口只指向独立实例。
- **预计涉及**：server/cmd、server/internal/auth 或 middleware 的实际入口、开发说明。

### LT-007 · 用浏览器打开 Web 工作台
- [ ] **状态：IN PROGRESS**；依赖：LT-006；规模：M；追踪：D10-V01、T-01。
- **范围/交付**：Web 连接独立 API，沿用上游页面与身份入口，关闭插件也可打开基础工作区。
- **验收**：无需 Electron；刷新不丢身份/空间上下文；API 断开显示错误，不伪报保存。
- **验证**：Web typecheck/build 与浏览器登录、刷新、断开场景；记录实际命令和页面证据。
- **预计涉及**：apps/web 配置、packages/core/api 或 auth 实际文件、开发说明。

### LT-008 · 提供实例启动、状态与停止入口
- [ ] **状态：TODO**；依赖：LT-004、LT-007；规模：M；追踪：T-01、NFR-04。
- **范围/交付**：为当前 Windows 实例提供可核验的启动/状态/停止方式，默认只启动日常需要组件，真实 daemon 按需启用。
- **验收**：重复启动不重复占端口；停止只影响本实例且保留数据；失败指出具体组件。
- **验证**：启动→浏览器访问→重复启动→状态→停止→重启；核对进程归属和数据保留。
- **预计涉及**：scripts/dev-start.ps1、dev-status.ps1、dev-stop.ps1（拟新增）、开发说明。

### CP-03 · W-01 出口
- [ ] LT-006～008 完成；浏览器入口可用，开发生命周期可复现，研究库不变，真实执行未过验证仍禁用。

## W-02：品牌、账号与授权

### LT-009 · 创建和切换品牌空间
- [ ] **状态：已交付，已合并，待用户验收**（PR [899ms/loretide#63](https://github.com/899ms/loretide/pull/63) 合入 app-main，2026-09-15）；依赖：LT-008、ARCH-02、D13-V12 接入合同（已闭合，#35）；规模：M；追踪：R-001、AC-001、D11-V01。已交付：工作区创建时可选时区（`settings["loretide.timezone"]`，零迁移，服务端 IANA 校验并显式拒绝 `""`/`"Local"`，缺省 `Asia/Shanghai`）、设置页显示与修改、切换后各自独立；`time/tzdata` 内嵌保证无系统 zoneinfo 的主机也能校验；`pnpm check:diagnostics-contract` 通过（本卡改的是上游 workspace，不新建 content 模块）。手动 UI 项 11 条见 app `specs/004-lt009-brand-workspace/manual-ui-todo.md`。遗留（非本卡）：`content_dispatch_outbox` 未登记进工作区删除清单（#45 遗留，已派修）。
- **范围/交付**：复用 Workspace 完成品牌名称、时区的创建、显示与切换，内容上下文从当前品牌派生。
- **验收**：创建 A/B 后刷新保持；切换后不显示前品牌数据；项目与品牌空间不混为同一对象。
- **验证**：品牌创建 API 定向测试＋浏览器切换；检查缓存键/事件的空间归属。
- **预计涉及**：workspace service/handler、packages/core/workspace、packages/views/workspace 对应文件。

### LT-010 · 建立内容对象的空间授权入口
- [ ] **状态：已交付，已合并，待用户验收**（PR [899ms/loretide#69](https://github.com/899ms/loretide/pull/69) 合入 app-main，2026-09-15）；依赖：LT-009；规模：M；追踪：NFR-03、AC-001、D10-V06。已交付：`server/internal/content/workspace-core/` 授权助手（从 diagnostics 提取，类型化判定，不缓存、每次判定重读成员关系）；diagnostics 改调助手且 403/404 逐字节不变；新模块规范映射「无权与不存在同为 404」，响应用诊断错误对象、不带正文；接入合同生效（`check:diagnostics-contract` 报 2 个已落地模块）；五条负例 + 存在性不泄漏断言；变异 5/5。无界面。
- **范围/交付**：给后续内容服务提供基于认证主体的品牌权限校验，定义无权读取/写入的统一行为。
- **验收**：不能凭传入 workspace_id 越权；拒绝结果不泄漏对象正文；不新增数据库外键/级联。
- **验证**：同用户不同品牌和另一身份的允许/拒绝测试，关联不存在及并发归属变化测试。
- **预计涉及**：server/internal/service 权限助手、handler 接入、针对性测试。

### LT-011 · 创建账号及平台配置
- [ ] **状态：已交付，已合并，待用户验收**（PR [899ms/loretide#71](https://github.com/899ms/loretide/pull/71) 合入 app-main，2026-09-15）；依赖：LT-010；规模：M；追踪：R-005～008、D11-V02。已交付：`content_account` 表（迁移 477/478，无外键，索引 `CONCURRENTLY`，平台受控枚举 Go 校验 + 库 `CHECK` 兜底）归 `ip-profile` 模块；创建/读取/更新接口经 workspace-core 授权，越权同为 404；同品牌多账号独立；已登记工作区删除清单；接入合同报 3 个已落地模块；变异 6/6（含补出的「双品牌成员递他品牌账号 id」负例）。无界面（页面在 LT-013）。
- **范围/交付**：实现品牌下 Account 与配置读写，保存平台、显示名及表达/内容形式设置；Account/ChannelAccount 是同一实体。
- **验收**：同品牌多个账号独立；非法平台拒绝；不存在独立人设表或绑定关系。
- **验证**：数据库迁移与定向服务测试，创建/读取/更新接口；sqlc 生成结果核对。
- **预计涉及**：server/migrations、pkg/db/queries、internal/service/handler 的最小账号切片；超5手写文件时先拆存储与API子任务。

### CP-04 · 品牌及账号合同
- [ ] LT-009～011 完成；账号归属和平台配置可查，品牌隔离负例通过，无独立人设模型。

### LT-012 · 保存账号人设提示词的配置版本
- [ ] **状态：已交付，已合并，待用户验收**（PR [899ms/loretide#73](https://github.com/899ms/loretide/pull/73) 合入 app-main，2026-09-15）；依赖：LT-011；规模：M；追踪：R-006/007、D11-V02。已交付：`content_account_revision` 只插不改的版本表（迁移 479～481，无外键，`(account_id, revision)` 唯一索引独立 `CONCURRENTLY` 文件，兼作并发互斥），四个端点（设置/读当前/读指定/列出）经 workspace-core 授权，并发冲突 409 可重试且重试有上限；空提示词有效并产生版本；旧版本按 `revision_id` 可读且内容不变；已登记工作区删除清单；content 三包 93/0/0。无界面（页面 LT-013）。
- **范围/交付**：账号配置增加 persona_prompt，变更保存配置版本；提供给后续运行读取的确定快照。
- **验收**：空提示词有效；改 A 不影响 B；旧版本可读，新运行才能采用新配置。
- **验证**：提示词空值/更新/历史读取测试，账户越权拒绝；不设计任务级 persona 参数。
- **预计涉及**：账号 query、service、输入schema及测试；需要迁移时单独记录生成文件。

### LT-013 · 账号设置页面可用
- [ ] **状态：已交付，已合并，待用户验收**（PR [899ms/loretide#79](https://github.com/899ms/loretide/pull/79) 合入 app-main，2026-09-15）；依赖：LT-012；规模：M；追踪：R-005～008、D11-V02。已交付：`/{workspaceSlug}/accounts` 页面（复用 `SettingsContent`/`SettingsCard`/`SettingsRow`/`SettingsSaveState`，零新增控件零样式改动），创建/切换账号、编辑平台与人设提示词；保存三态文案（成功/失败/409「他人同时修改，再保存一次即可」）；换账号丢弃未保存输入（core 纯函数锁住）；平台枚举由测试读 `account.go` 比对。**顺带补上 #71/#73 从未挂载的 8 个端点**（`upstream:` 独立提交 + 路由存在性用例）。任务卡的「页面组件测试」按 constitution 原则 II 改为 core 纯函数测试 + 9 条手动项（`specs/017-lt013-account-settings-page/manual-ui-todo.md`）。**到此 docs/01 §3.1「账号表达配置」的最小切片在浏览器里可操作**（平台、显示名、人设提示词）；§3.1 完整字段与 Agent 提炼候选档案仍未做。
- **范围/交付**：浏览器在品牌下创建/切换账号，编辑平台要求与人设提示词，不提供人设库或绑定页面。
- **验收**：保存/失败状态明确；刷新保持；换账号不沿用前账号未保存输入。
- **验证**：页面组件测试、类型检查和浏览器双账号场景；插件关闭仍可用。
- **预计涉及**：packages/core/content 账号接口、packages/views/content 账号视图、apps/web 路由（均按实际约定新增）。

### LT-014 · 保存账号资料范围偏好
- [ ] **状态：TODO**；依赖：LT-013；规模：M；追踪：R-023/025、开发基线§5。
- **范围/交付**：定义 local/web/all 范围，账号首次默认 all，保存上次开始界面的选择；先提供可测试设置接口，开始界面在 EP-04 接入。
- **验收**：账号间偏好独立；非法值拒绝；异常继续使用独立字段/尝试，不能回写偏好。
- **验证**：默认值、账号切换和异常回写拒绝测试；运行快照接口可表达固定值。
- **预计涉及**：账号配置 query/service、core 类型及测试。

### CP-05 · 账号表达与偏好
- [ ] LT-012～014 完成；浏览器保存提示词，历史配置不变，资料范围默认和独立保存正确。

### LT-015 · 品牌统一自动预检开关
- [ ] **状态：TODO**；依赖：LT-013；规模：M；追踪：R-038/039、D11-V06。
- **范围/交付**：品牌配置默认自动预检开启及设置界面，所有账号统一使用；此处只做配置，预检运行由 EP-06 实现。
- **验收**：新品牌 true；关闭可保存但不表示免人审；没有账号级覆盖字段或控件。
- **验证**：默认/切换/跨品牌配置隔离和浏览器设置测试；不调用模型。
- **预计涉及**：品牌设置存取、core 契约、views 设置组件及测试。

### LT-016 · 建立素材与任务授权范围合同
- [ ] **状态：TODO**；依赖：LT-010、LT-012；规模：M；追踪：R-001/025、D11-V01。
- **范围/交付**：定义 brand/account 归属与任务 Grant，提供资产读取前可复用校验；任务工作流稍后接入，不创建全套引擎。
- **验收**：同品牌账号专属资料无授权不可读；提示词相同不扩权；无效/撤销授权拒绝读取。
- **验证**：权限矩阵夹具、资源/账号/品牌不一致及撤销测试，记录缓存失效要求。
- **预计涉及**：content 授权 contract/service、针对性测试；存储扩展在首个资源切片落实。

### CP-06 · W-02 出口
- [ ] LT-015～016 完成；品牌统一开关可用，账号资料访问合同明确，身份提示词与访问权限彻底分开。

## W-03：本地素材接入与知识

### LT-017 · 证明浏览器主动选本地文件路径
- [ ] **状态：TODO**；依赖：LT-008、LT-016；规模：M；追踪：R-002、AC-036、NFR-03。
- **范围/交付**：实现最小的受控本机选择/登记通道，浏览器取得不可伪造的选定文件引用，不把任意绝对路径当授权。
- **验收**：取消不登记；仅选定文件可访问；无 Electron 前置、无目录扫描监听、无云端上传。
- **验证**：Windows 实际选择、取消、伪造引用与未授权路径访问；核验协议/API来源约束。
- **预计涉及**：本机服务选择接口、Web 选择适配、协议类型及测试；先记录浏览器能力限制，再决定具体实现。

### LT-018 · 保存带归属的资产登记
- [ ] **状态：TODO**；依赖：LT-017；规模：M；追踪：R-002/009、D11-V01。
- **范围/交付**：保存资产 ID、brand/account 归属、选定路径引用、大小/类型/哈希与登记状态；同文件引用与物理文件分开。
- **验收**：重复提交幂等；同路径不等于相同内容；不移动/复制原成品或泄漏其他账号元数据。
- **验证**：重复登记、错误归属、读取失败测试，迁移/sqlc与事务检查。
- **预计涉及**：资产 query/service、迁移、登记 handler及测试；超5手写文件先拆子任务。

### LT-019 · 在浏览器预览登记资产
- [ ] **状态：TODO**；依赖：LT-018；规模：M；追踪：R-002/036、AC-036。
- **范围/交付**：文本、支持的图片/视频预览与状态显示，受控读取已登记文件，不要求新增托管副本。
- **验收**：权限检查在返回内容前；格式不支持显示说明；视频读取支持实际所需 Range，不把失败标预览成功。
- **验证**：浏览器文字/图片/视频样例，非法ID、越权、Range边界与不可读文件测试。
- **预计涉及**：本机资产读取 handler、core 资产客户端、views 预览及测试。

### CP-07 · 选择、登记、预览
- [ ] LT-017～019 完成；浏览器主动选文件到预览可用，未扫描目录、复制成品或扩大账号读取权限。

### LT-020 · 检测文件缺失与内容变更
- [ ] **状态：TODO**；依赖：LT-019；规模：M；追踪：R-002/039、D11-V07。
- **范围/交付**：打开/提审/交付可调用按需校验，保存当前可读性及与登记哈希的差异；不运行后台监听。
- **验收**：覆盖同名文件标已变更；删除标缺失；系统不承诺恢复旧文件，旧哈希不被覆盖成当前审批依据。
- **验证**：修改、删除、权限失效和读取中变化测试；大文件校验支持可见状态/取消，避免阻塞界面。
- **预计涉及**：资产校验 service、查询字段、预览状态和测试。

### LT-021 · 人工重新关联移动的文件
- [ ] **状态：TODO**；依赖：LT-020；规模：M；追踪：R-002、AC-002、D11-V07。
- **范围/交付**：操作人选择新位置重新关联，记录旧路径引用与新引用；按哈希区分移动与换内容。
- **验收**：同哈希且其他条件有效可续用引用；不同哈希产生变更状态；取消不丢原记录。
- **验证**：移动/改名、替换成别文件、越权重关联和取消的浏览器及服务测试。
- **预计涉及**：重关联 service/handler、core接口、views控件及测试。

### LT-022 · 解析首条文字来源并保留出处
- [ ] **状态：TODO**；依赖：LT-018；规模：M；追踪：R-009～013、015。
- **范围/交付**：先完成 TXT/Markdown 或原生粘贴文本之一的解析切片，保存来源、内容、定位及解析状态；其他格式在其后扩展，原需求不删除。
- **验收**：原文和派生摘要分开；解析失败保留来源；正文可追溯到选定文件版本/文本输入。
- **验证**：中文、空文本、编码/解析失败、重复解析幂等样例；不调用模型即可验证来源保存。
- **预计涉及**：来源 parser/service、来源存取、详情展示及测试。

### CP-08 · 文件变化与首条解析
- [ ] LT-020～022 完成；文件覆盖/缺失及人工重关联可处理，首条文字来源可定位，未添加自动媒体副本。

### LT-023 · 检索授权知识并应用必用/排除
- [ ] **状态：TODO**；依赖：LT-016、LT-022；规模：M；追踪：R-015～019、025、开发基线§4。
- **范围/交付**：先提供中文关键词检索与候选清单，权限/排除在正文返回前生效；必用素材不可用时返回明确冲突。
- **验收**：中文实际可命中；排除/未授权材料不在摘要结果中；必用不成为越权通道，不伪报使用。
- **验证**：跨账号/跨品牌负例、中文命中、必用与排除冲突、资料缺失和同来源去重夹具。
- **预计涉及**：知识 query/service、输入输出契约、针对性测试；不用向量库作为首条检索前置。

### LT-024 · 浏览器素材库串联及 W-03 验证
- [ ] **状态：TODO**；依赖：LT-021、LT-023、LT-015；规模：M；追踪：R-009～019、AC-002、D11-V01。
- **范围/交付**：把登记/归属/预览/检索结果呈现在同一素材入口，显示必用/排除可供后续开始界面使用的状态。
- **验收**：两个账号的资料可按授权查找且不会串用；刷新保留；缺失/变更可处理，没有虚构 AI 摘要。
- **验证**：浏览器完整路径＋受影响 Web/Go 检查；导出应用数据与引用清单后验证缺失本地文件提示。
- **预计涉及**：views/content 素材页面、core/content聚合接口、Web 路由、浏览器用例及验证记录。

### CP-09 · W-03 出口
- [ ] CP-08 和 LT-023～024 完成，核对授权检索与素材库串联场景；所涉 Go/Web 检查通过，浏览器证据齐全。此检查点不代表后续创作和审核已完成。

## 后续阶段项：进入阶段前再拆细

### EP-04 · 开始界面、选题与 SOP
- [ ] **状态：TODO（阶段项，不可直接实施为单任务）**；依赖：CP-09；追踪：W-04、R-020～025/049、D11-V03。
- **范围/交付**：候选选题/自定义简报、研究/改写、三资料范围和偏好接入、必用/排除、受约束 SOP 及输入快照。
- **出口/验证**：开始后配置固定，非法依赖拒绝，项目可选；浏览器和配置合同验证。先拆开始界面、SOP模型、依赖校验与运行快照等独立卡片。

### EP-05 · 可控执行与正式作品
- [ ] **状态：TODO（阶段项）**；依赖：EP-04；追踪：W-05、R-025～029/031～037。
- **范围/交付**：持久化步骤、正文前研究确认、文本编辑/AI候选版本、取消/重试/迟到处理；扩展 DOCX/PDF/转录等既有 P0 解析要求。
- **出口/验证**：可控执行器跑完整创作和编辑链，失败不丢成果、迟到不覆盖；模拟数据不计真实结果。先细拆编辑与编排，不把整个引擎作为一项。

### EP-06 · 两上游审核能力与人工终审
- [ ] **状态：TODO（阶段项）**；依赖：EP-05；追踪：W-06、R-028/038～040、D11-V04～08/11。
- **范围/交付**：规则/许可映射、精简 Skill、报告契约、不可变 AI 发现、逐项处置、未完成继续及后端终审门禁。
- **出口/验证**：自动开关不能取消人审；AI blocker 可人工处置，系统权限/版本/必需附件门禁不可绕过；至少30差异样例，模型质量单独评估。先按存储、调度、处置、审批、界面拆卡。

### EP-07 · 手动交付记录与复盘
- [ ] **状态：TODO（阶段项）**；依赖：EP-06；追踪：W-07、R-041～051、D11-V09。
- **范围/交付**：审核包/排期草案、人工发布与真实指标、AI复盘成果、人工采纳记忆和项目组织。
- **出口/验证**：导出不等于发布，缺失不是零，长期记忆不自动写；两个账号数据和复盘范围隔离。先按交付、记录、复盘、采纳拆卡。

### EP-08 · Codex 首条真实联调
- [ ] **状态：TODO（阶段项）**；依赖：EP-07、LT-004；追踪：W-08、R-030、NFR-03/07/08。
- **范围/交付**：落实执行器风险清单；移除交付工程 OpenClaw 依赖/入口/回退，修复认证复制与权限缺口；验证 Codex 及其实际搜索能力，串联研究/改写、预检、复盘。
- **出口/验证**：本地模式不联网查资料；全部联网失败遵循人工恢复；越权访问拒绝；有真实浏览器和模型输出证据。先拆安全修复、客户端接入、搜索约束、故障与E2E，不把阶段当单任务。

### EP-09 · 远程恢复与替换执行器
- [ ] **状态：TODO（阶段项）**；依赖：EP-08；追踪：W-09、R-030/052～055、D11-V10。
- **范围/交付**：远程配对、租约/重启/重传与迟到结果、跨机文件授权协议；其他 Agent/API/MCP 按实际支持矩阵接入。
- **出口/验证**：每条路径记录版本、认证类型和实际通过范围；API至少两个供应商，不能以 Codex 代替其他；桌面仅做受影响专项。进入时按协议、恢复故障和每个适配器拆卡。

## 更新记录模板

每项完成时追加：日期、实现仓库/提交、实际文件、已运行命令及退出码、浏览器证据、需求/验收关联、剩余限制。若拆子任务，保持父任务不勾选，直到所有子任务和验收完成。所有状态更新随对应文档提交保存。

## 模块化共同完成标准

每项必须遵守 [模块边界与变更回归约束](../docs/12-模块边界与变更回归约束.md)，记录所属模块、公开契约变化、受影响消费者、迁移和实际回归。仅目录分开或单模块测试通过不能证明其他功能不受影响。新增功能从 LT-009 起以前置 ARCH-02 通过为条件。

## 完整诊断的初期交付要求

LT-009 的开工前置自 2026-09-15 起改为 D13-V12 接入合同（已闭合），DIAG-01～13 的其余部分随后续交付逐步补（见 plan.md「优先级决定」）。每项后续功能必须同时接入操作日志、技术错误、trace、输入快照和对应故障/回归验证，参照 [D13验收](../docs/13-完整开发诊断与操作日志需求.md)，不准延后到收尾。原EP-05可控执行器复用DIAG-11并补业务步骤，不能重复造第二套模拟协议。

远程环境证据：API/Web代理200、未认证401、迁移重跑成功见 [服务联通与迁移复核](../records/2026-09-13-Docker服务联通与迁移复核.md)；浏览器登录、工作区渲染和刷新已通过，其他验收仍未完成。单页面冷编译见 [峰值与耗时](../records/2026-09-13-开发冷编译峰值与耗时.md)。附件目录重建保留已通过；用户指定迁往新 VPS，主机身份待独立核对，见 [附件持久化与新机准备](../records/2026-09-13-附件持久化与新开发服务器准备.md)。
